## 已实现功能清单（按模块）

### 2.1.1 课程管理模块

- **课程创建**：教师创建课程（含分类、发布状态、开课/结课时间）
- **课程编辑**：教师编辑课程信息（标题/简介/分类/发布/时间安排/知识图谱 JSON）
- **学生选课**：学生选课（Enrollment）
- **课程资料/讲义**：
  - 资料上传（multipart）
  - 资料列表、下载、删除
  - 文件落盘到 `backend/uploads/`
- **班级管理**：
  - 创建班级、查看班级
  - 班级改名、删除班级（删除后学生变为未分班）
  - 教师为选课学生分班（更新 `enrollment.classId`）
  - 教师查看课程学生名单
- **课程知识图谱**：
  - 根据课程标题/简介/实验标题生成简易知识图谱 JSON
  - 保存到课程，并在课程页展示概览
- **课程分类显示**：
  - `GET /courses/categories` 返回分类列表
  - 课程中心支持分类筛选（前端下拉）

---

### 2.1.2 在线实验模块

- **实验创建**：教师创建实验（语言、说明、`starterCode`）
- **实验环境**：学生登录后进入实验页（Monaco Editor）
- **代码编辑**：在线编辑 + 提交评测
- **文件管理（实验附件）**：
  - 教师上传实验附件（multipart）
  - 学生/教师下载附件
  - 教师删除附件
- **AI 问答助手（本地版）**：
  - `POST /labs/:labId/ai-help`：基于实验描述 + 公开样例生成提示（不依赖外部服务）
  - 前端实验页可提问并展示回答

---

### 2.1.3 自动评测模块

- **测试用例管理**：
  - 教师新增测试用例（含 `hidden` / `weight`）
  - 教师查看全部用例
  - 教师修改 / 删除用例
  - 前端实验页提供测试用例管理 UI（教师）
- **自动运行**：
  - 提交进入 BullMQ 队列（Redis）
  - `judge-worker` 执行 JS(Node)/Python3 代码并回写结果
- **评分生成**：按用例权重计算分数，写回 `Submission.score/status/resultJson`
- **详细反馈**：
  - `GET /submissions/:id/feedback` 学生友好反馈（对学生隐藏 hidden 用例 I/O）
  - 前端实验页展示 feedback
- **防作弊检测（可选高级功能）**：
  - `GET /submissions/:id/similarity` 启发式相似度 Top10（教师/管理员）
  - 前端按钮触发并展示结果

---

### 2.1.4 作业管理模块

- **作业创建**：教师创建作业（支持按班级发布 `targetClassId`、截止时间、发布状态）
- **作业发布/撤回**：教师发布/撤回作业
- **作业提交**：学生提交/多次修改（upsert）
- **作业批改**：教师评分与反馈
- **成绩发布**：
  - 批改后默认不对学生可见（`released=false`）
  - 教师一键发布已批改成绩（`released=true`）
  - 学生端仅在 released 后看到分数/反馈
- **问答管理（作业内）**：
  - 学生提问
  - 教师回答
  - 课程页展示作业内问答区

---

### 2.1.5 成绩统计模块

- **成绩查看（学生）**：
  - `GET /grades/me` 按课程返回总评、排名、班级规模、权重
  - 前端“我的作业”页展示课程总评/排名
- **成绩统计（教师）**：
  - 成绩册返回每位学生：实验均分 / 作业均分 / 总评 / 排名
  - 总评分布统计（<60、60-69、70-79、80-89、90+）
- **成绩导出**：
  - 成绩册导出 CSV（增强列：均分/总评/排名等）
- **成绩权重**：
  - 课程支持配置实验/作业权重（和=1）
  - 成绩册页可保存权重并重算
- **AI 批改（作业辅助）**：
  - `POST /homework/submissions/:sid/ai-suggest`（预览建议/一键应用）
  - 课程页教师“批改台”支持 AI 建议与应用

---

### 2.1.6 用户管理模块

- **注册/登录**：邮箱注册、登录、JWT 会话
- **个人信息**：
  - 个人中心页：修改姓名、头像 URL
  - 修改密码（需旧密码）
- **权限管理**：
  - STUDENT/TEACHER/ADMIN 权限控制（路由级）
  - 管理员用户列表页（`GET /admin/users` + 前端页面）
- **身份验证（演示版）**：
  - 发送验证令牌/验证邮箱（token hint）
  - 忘记密码/重置密码（token hint）
  - 顶部导航显示“邮箱未验证”提示

---

## 2.2 非功能需求（已落地的部分）

- **性能/可扩展**：无状态 API + Redis 队列化评测 + 可多 worker（结构已具备）
- **可用性**：健康检查
  - `GET /health/live`
  - `GET /health/ready`（DB+Redis）
- **安全性**：基础安全响应头（helmet）、全局与关键接口限流、请求体大小限制、`X-Request-ID`
- **压测冒烟**：根目录 `npm run perf:smoke`（可调并发/请求数/目标 URL）

