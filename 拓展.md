# 修改1：教师创建实验的页面与逻辑修改。

具体修改要求如下：
1. 老师课程界面不再是直接创建实验，而是创建“实验集/综合实验”，每次综合实验分为多个题目。教师可从课程界面跳转到实验管理界面。

2. 实验管理界面包括（1）题目列表、删除选项（2）完成率等信息的统计（3）每个学生的完成情况统计（4）右上角创建新题目，进入题目创建弹窗（5）实验截止时间设置，截止后学生不可提交。

3. 题目创建弹窗应该有以下内容：（1）题目名称。（2）支持md语言编辑的题干描述（要求有预览）。（3）在线IDE初始代码设置（4）测试示例选择（5）测试用例的上传/创建位置（6）语言限制（7）题目完成情况统计。

4. 实验完成情况统计应包括：
（1）学生信息（2）所有题目测试用例通过情况（3）学生最后一次提交时间（4）总罚时

# 修改2：学生实验界面的修改：
1.学生界面保留实验列表，上显示截止时间，但是要求跳转到“实验集”界面，内
显示题目列表和测评通过情况。点击题目进入做题界面
2. 做题界面布局修改为类似leetcode。左侧为实验名称和题干描述（编译后的md）。右侧上部为在线IDE。右侧下部为多页面窗口，分别为示例、测评结果、AI助手和讨论区。

# 修改3：AI
AI采用问答形式，采用openai统一接口接入deepseek的api.

# 修改4：
后端逻辑的对应修改




对照 拓展.md 与现有数据模型，梳理后端改造范围并拆分步骤。

下面按你的《拓展.md》从后端视角说明要做什么、动哪些文件，以及建议的分步实施与每步测试（前端可并行，这里以 API/数据为准）。

1. 实现这些修改，后端要做什么（结论）
核心变化是：从「课程 → 多个平铺 Lab」变成「课程 → 实验集（带截止时间）→ 多个题目（Lab）」，并叠加统计、罚时、截止后禁止提交、真 LLM 问答、题目级讨论区。

后端必须覆盖的大类如下。

需求块	后端职责
实验集
新实体（名称建议 LabSet / ExperimentPack）：归属 courseId、title、dueAt（可选 description）、排序等；课程页只创建/列出实验集，不再直接创建平铺 Lab。
题目
现有 Lab 改为挂在实验集下：增加 labSetId（并处理与现有 courseId 的关系，见下文「迁移策略」）；题干支持 Markdown：增加 descriptionMd（Text），可与旧 description 并存或逐步废弃。
截止提交
在 POST /labs/:id/submit（或你改名后的提交接口）中读取题目所属实验集的 dueAt，now > dueAt 则 403；可选：教师/管理员仍可提交或单独策略。
统计
新只读接口：实验集维度「完成率、每题通过率」；学生维度「每题是否全过/最高分、最后一次提交时间、总罚时」；题目弹窗内「该题完成情况」可复用子集字段。
罚时
需明确定义（见下）；在 Submission 或聚合查询中可算：例如以实验集 openedAt/publishedAt 为起点，每题首次 AC 前每次非 AC 计一次罚分（常见为 20 分钟）再累加每题 AC 相对起点的时间。若产品要「和 LeetCode 一致」需你确认规则，后端再落字段。
测试用例上传
若「上传」指文件：新增 multipart 解析（如 CSV/JSON 行协议），批量写入 TestCase；与现有 POST .../testcases 并存。
AI（DeepSeek + OpenAI 兼容）
新配置：OPENAI_API_KEY、OPENAI_BASE_URL（DeepSeek）、OPENAI_MODEL；新路由：多轮 messages 问答，服务端拼 system prompt（含题目 MD 摘要、禁止泄露 hidden 用例等）；限流、超时、错误脱敏。
讨论区
现有 DiscussionPost 仅 courseId；需题目级（或实验集级）维度：扩展 labId/labSetId 可空 + 索引，并提供 GET/POST 列表与发帖接口（权限：选课或教师）。
成绩册
grades.ts 里当前按 course.labs 扁平算实验均分；改为按「实验集 → 多题」聚合（例如每集内题目均分再对集平均，或集内总题数加权），避免统计重复或漏题。
迁移策略（二选一，实施前定案）

A（推荐）：Lab 增加必填 labSetId，courseId 保留作冗余便于权限/查询，由应用层保证与 labSet.courseId 一致；数据迁移：为每个旧课程建默认实验集，把现有 Lab 挂上去。
B：labSetId 可选，新数据必填；旧接口兼容一段时间（技术债高，不推荐长期）。
2. 要改哪些文件、改什么内容
文件	修改内容
backend/prisma/schema.prisma
新增 LabSet；Lab 增加 labSetId、descriptionMd；DiscussionPost 增加 labId? / labSetId?（或单独 LabDiscussion 表）；视罚时规则给 LabSet 增加 penaltyStartAt 等；Course 上 labs 关系改为通过 labSets → problems 或保留双关系迁移期。
backend/prisma/migrations/*
新迁移 + 数据回填脚本（若有旧数据）。
backend/prisma/seed.ts
种子数据改为「课程 → 实验集 → 多题」。
新建 backend/src/routes/lab-sets.ts（或并入 courses.ts）
CRUD 实验集、PATCH dueAt、列表（含题目数/完成率摘要可选）、教师跳转管理页所需聚合。
backend/src/routes/labs.ts
创建题目改为在 labSetId 下创建；GET 返回 descriptionMd；提交前校验截止时间；列表 GET /courses/:id/labs 可能改为 GET /lab-sets + GET /lab-sets/:id/problems。
backend/src/routes/lab-files.ts
若附件仍挂在「题目」上，可只改权限校验链（从 course 经 labSet 校验）；或支持实验集级附件则加 labSetId 与存储路径。
backend/src/routes/discussions.ts（或新建 lab-discussions.ts）
按 labId 筛选/发帖；鉴权与课程选课一致。
backend/src/routes/ai-help.ts
改为调用 OpenAI 兼容客户端（或新建 ai-lab-chat.ts 保留旧接口作 fallback）。
新建 backend/src/lib/llm.ts 或 openai-client.ts
fetch 或 @openai/openai 调 DeepSeek base URL；流式可选。
backend/src/lib/config.ts
读取上述环境变量。
backend/src/index.ts
注册新路由插件。
backend/package.json
如需官方 SDK 则加依赖。
judge-worker/src/worker.ts
若提交仍用 submission.labId，逻辑可不变；若增加「仅跑公开用例」需新 job 类型另议。
backend/src/routes/grades.ts + 若有 CSV 导出
实验分按实验集/题目新结构重算。
backend/src/routes/courses.ts
GET /courses/:id 的 include：返回 labSets 而非直接 labs（或两者过渡）。
前端、Vite 代理、CourseDetail.tsx、Lab.tsx 等你在其它步改；后端可先提供稳定 API 再对接。

3. 建议分步修改与每步测试
下面顺序尽量先数据与 API，再统计与 AI，避免大面积返工。

步骤 1：数据模型与迁移（LabSet + Lab.labSetId + descriptionMd）
做：Prisma 新增 LabSet；Lab 增加 labSetId、descriptionMd；迁移 + seed 调整；Course 与 LabSet 关联。
测：npx prisma migrate deploy（或 dev migrate）无报错；prisma db seed 成功；数据库中能看到「集 → 多题」层级。
步骤 2：实验集 CRUD + 课程侧列表（教师/学生可读范围）
做：POST/PATCH/GET/DELETE /courses/:courseId/lab-sets（路径可按你命名习惯）；列表含 dueAt、题目数量。
测：Postman/curl：教师创建/改截止/删；学生/未选课访问 403/404 符合预期；管理员全权限。
步骤 3：题目（Lab）挂到实验集 + 提交截止校验
做：创建题目接口带 labSetId（或 POST /lab-sets/:id/problems）；submit 前查 dueAt；旧 POST /courses/:courseId/labs 删除或 301 到新模式。
测：截止前提交成功并入队；截止后学生提交返回明确错误码；教师是否可提交按产品二选一测通。
步骤 4：统计接口（完成率、按学生、按题、最后提交时间、罚时）
做：例如 GET /lab-sets/:id/stats（汇总）+ GET /lab-sets/:id/students-progress（表格数据）；罚时按已定公式在服务端纯计算或写物化字段。
测：用 seed 多用户、多提交造数据，核对 JSON：每题通过情况、最后一次 createdAt、罚时与手算一致。
步骤 5：讨论区（题目维度）
做：扩展 DiscussionPost 或新表；GET/POST /labs/:labId/discussions（或挂在 lab-set 下）。
测：未选课不能发帖；本课程学生可见同题帖子；教师可见。
步骤 6：AI（OpenAI 兼容 → DeepSeek）
做：config + POST /labs/:labId/ai-chat（messages[]）；系统提示词防泄题；全局限流。
测：无 key 时优雅降级；有 key 时多轮对话成功；超时与 429 处理；费用相关不在此展开但建议打日志 request id。
步骤 7：成绩册与兼容收尾
做：grades.ts（及导出）按新层级统计实验分；GET /courses/:id 返回结构与前端约定一致。
测：成绩册中实验均分与「实验集内多题」规则一致；旧数据（若已迁移）抽样对比迁移前后总分误差可接受。
