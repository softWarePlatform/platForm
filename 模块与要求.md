# 在线教学与实训平台 — 模块与要求

本文档按业务模块列出能力要求，供设计与实现对照。不包含人员分工。

---

## 1. 用户与身份

**目标**：可信的身份与权限，支撑师生与管理员不同操作范围。

**要求**：

- 支持注册、登录、安全退出；会话在服务端可校验（如令牌机制）。
- 区分至少三种角色：学生、教师、管理员；接口与页面按角色授权。
- 密码加密存储，禁止明文落库；敏感操作需二次校验（如改密验证旧密码）。
- 个人信息可维护（姓名、联系方式或头像等，按产品约定字段）。
- 管理员可对用户进行必要治理（如查询、禁用、角色调整等，具体以课程范围为准）。
- 可选：邮箱验证、找回密码；若启用，应走真实可达渠道或课程规定的演示方案，并在文档中说明边界。

---

## 2. 课程与教学组织

**目标**：教师能建课、组织班级与资源；学生能发现课程并完成选课。

**要求**：

- 课程的创建、编辑、发布/下架；未发布课程对学生不可见或不可选（规则需一致）。
- 学生选课、退课（若需要）及选课人数或时间限制（按需求可选）。
- 班级管理：创建班级、重命名、删除；将选课学生划入班级。
- 课程资料：上传、列表、下载、删除；权限为教师管理、学生只读；文件大小与类型限制明确。
- 教学内容结构：至少支持「章节 / 单元」或等价结构，资料与作业可挂到对应结构（若课程不要求章节，可退化为单层资料列表，但须在需求中写死一种）。
- 可选：课程公告（与讨论区区分：公告由教师发布、全员可见）。

---

## 3. 在线实验

**目标**：学生在浏览器内完成编程类实验并提交。

**要求**：

- 教师为课程设置实验：标题、说明、截止时间（若需要）、起始代码、允许语言列表。
- 学生端在线编辑与提交；提交与实验绑定、与学生身份绑定。
- 实验附件（指导书、数据文件等）：教师上传、学生下载；删除与权限控制清晰。
- 实验列表在课程内可见；未选课学生不可进入实验页。

---

## 4. 自动评测

**目标**：对学生代码进行自动判分，结果可追溯、反馈合理。

**要求**：

- 教师维护测试用例：输入、期望输出、分值权重；支持公开样例与隐藏用例。
- 提交异步执行，状态可查（排队中、判题中、已完成等）；避免阻塞整站请求线程。
- 判题结果含得分与简要原因；对学生隐藏隐藏用例的敏感细节。
- 超时与异常（运行错误、答案错误）区分清楚。
- 执行环境需有基本资源限制（时间、内存、输出长度等），降低恶意代码影响；课程项目可声明为「演示级」但须在文档写明局限。
- 可选：多语言支持、代码相似度辅助分析（不作为唯一作弊认定依据）。

---

## 5. 作业

**目标**：教师布置、学生提交、教师批改与反馈闭环。

**要求**：

- 作业创建、编辑、发布/撤回；截止时间；可按班级定向发布（若需要）。
- 学生提交；支持多次修改或仅一次提交（规则统一并在界面提示）。
- 提交形态需与需求一致：纯文本、富文本或附件（若含附件，需上传、病毒扫描或类型白名单等按课程要求取舍）。
- 教师批改：分数、评语；批改后是否对学生默认可见须有明确规则（如「发布成绩」动作）。
- 可选：作业内提问与教师答复（线程或列表均可，规则一致即可）。

---

## 6. 成绩与统计

**目标**：成绩可算、可查、可汇总，减轻教师手工统计。

**要求**：

- 实验成绩、作业成绩进入课程总评的规则可配置（如权重之和为 1）；修改规则后可重算或说明生效范围。
- 学生查看本人课程成绩与排名（若展示排名，需说明班级范围或课程范围）。
- 教师查看全班成绩册：分项成绩、总评、简单分布（分段或图表均可）。
- 成绩导出（如 CSV 或 Excel），字段与系统展示一致。
- 数据一致性：删除课程/作业/实验时，关联成绩与统计的处理策略明确（级联或禁止删除）。

---

## 7. 交流与通知（可选模块，若纳入范围则单独满足）

**目标**：师生异步沟通；重要节点可触达用户。

**要求**：

- 课程讨论区或留言：发帖、查看；若需要回复，则支持层级或引用。
- 站内通知或邮件：作业发布、截止提醒、批改完成等事件可按配置触发；已读未读（若做通知中心）。
- 系统与业务日志：关键错误可定位；重要操作可审计（按课程深度选做）。

---

## 8. 非功能与交付（横切要求）

**目标**：系统可演示、可部署、可说明局限。

**要求**：

- 性能：核心页面加载与常用接口在课程约定环境下达到约定指标（如页面首屏、并发用户数）。
- 可用性：部署后基本健康检查；依赖故障时有明确错误提示。
- 安全：HTTPS（若部署环境提供）、常见 Web 漏洞防护思路在设计与实现中有对应措施（鉴权、输入校验、权限校验等）。
- 兼容：约定支持的浏览器与分辨率；移动端若列为需求则需单独验收标准。
- 文档：需求说明、设计说明、测试说明、部署与用户说明与代码行为一致；接口说明建议可用 OpenAPI 或等价形式便于联调。

---

## 9. 模块一览（便于目录对照）

| 序号 | 模块 |
|------|------|
| 1 | 用户与身份 |
| 2 | 课程与教学组织 |
| 3 | 在线实验 |
| 4 | 自动评测 |
| 5 | 作业 |
| 6 | 成绩与统计 |
| 7 | 交流与通知（可选） |
| 8 | 非功能与交付（横切） |
