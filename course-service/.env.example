# 首日拆分阶段：只读连接现有演示数据库，用于验证 course-service 可独立启动。
# 9 月 1 日任务会替换为课程服务独立数据库或 schema，并提供专属迁移。
DATABASE_URL=postgresql://platform:platform@localhost:55432/teaching_platform?schema=public
PORT=3001
CORS_ORIGIN=http://localhost:5173,http://localhost:8080
# 后续认证、内部接口和资料迁移使用；不得提交真实值。
JWT_SECRET=replace-with-long-random-secret
INTERNAL_SERVICE_TOKEN=replace-with-service-token
UPLOAD_DIR=./uploads
REQUEST_ID_HEADER=x-request-id
