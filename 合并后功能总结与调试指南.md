# 合并后功能总结与调试指南

本文基于当前工作区代码（`backend`、`frontend`、`judge-worker`、根目录脚本与 `RUN.txt`）整理，描述已实现能力、实现方式、性能关注点及建议测评方式。

---

## 一、项目概览

本仓库为**在线教学与实训平台**：学生选课、做编程实验（在线编辑器提交、异步评测）、交作业、看成绩与讨论；教师管理课程、实验集与题目、作业批改与成绩册；管理员可管理用户。数据层为 **PostgreSQL（Prisma）**，实验评测为 **Redis + BullMQ 队列 + 独立 judge-worker**，HTTP API 为 **Fastify**，前端为 **React + Vite**，开发时通过 Vite 将 `/api` 代理到后端。

---

## 二、功能列表（含详细描述）

### 2.1 账户与权限

| 功能 | 详细描述 |
|------|----------|
| 注册 / 登录 | 邮箱注册（可选学生/教师角色）、登录返回 JWT；密码 bcrypt 存储。 |
| 当前用户 | `GET /auth/me` 获取资料；支持可选登录场景（部分课程接口）。 |
| 资料与密码 | 修改昵称、头像 URL；修改密码（需旧密码）。 |
| 邮箱验证（演示向） | 生成验证 token 的接口直接返回 `verifyTokenHint`（无真实发信）。 |
| 忘记密码（演示向） | 申请重置返回 `resetTokenHint`；凭 token 重置密码。 |
| 角色与路由守卫 | 角色：`STUDENT` / `TEACHER` / `ADMIN`。前端 `RequireAuth` / `RequireRole` 控制页面；后端各路由 `authRequired` / `optionalAuth`。 |
| 管理员用户列表 | `GET /admin/users`（管理员）；前端 `/admin/users`。 |

**实现要点**：`backend/src/routes/auth.ts`、`backend/src/lib/jwt.ts`、`backend/src/lib/password.ts`、`backend/src/lib/authGuard.js`；前端 `frontend/src/auth/AuthContext.tsx`、`frontend/src/App.tsx`。

---

### 2.2 课程与选课

| 功能 | 详细描述 |
|------|----------|
| 课程列表与分类 | 公开课程列表、分类筛选；未发布课程仅教师/管理员可见。 |
| 课程详情 | 标题、描述、教师、开课结课时间、知识图谱 JSON（若有）、实验与作业入口等。 |
| 选课 / 退课 | 学生选课；可关联班级（`Enrollment.classId`）。 |
| 课程管理（教师） | 创建/更新/删除课程、发布、维护班级、设置知识图谱 JSON、总评权重（实验 + 作业 = 1）。 |
| 课程资料 | 教师 multipart 上传资料；选课学生或教师可列表与下载（未发布课程对非教师隐藏）。 |

**实现要点**：`backend/src/routes/courses.ts`、`backend/prisma/schema.prisma`（`Course`、`Class`、`Enrollment`）；资料 `backend/src/routes/course-materials.ts`、`backend/src/lib/uploads.js`；前端 `Courses.tsx`、`CourseDetail.tsx`、`CourseManage.tsx`。

---

### 2.3 在线实验（实验集 + 单题 Lab）

| 功能 | 详细描述 |
|------|----------|
| 实验集（LabSet） | 一门课下多个实验集，含标题、说明、截止时间、排序；教师创建/改/删；删空集或带 `force` 删有题集。 |
| 实验题目（Lab） | 归属某一实验集；语言 `javascript` / `python`；题干 `description` + **Markdown 题干** `descriptionMd`；初始代码 `starterCode`。 |
| 测试用例 | 教师维护用例：输入/期望输出、是否隐藏、权重；无隐藏用例时 AI 与反馈仍可用公开信息。 |
| 实验附件 | 每题可上传多个 `LabFile`（存储路径 + 元数据）。 |
| 学生实验页 | Monaco 编辑器、提交、轮询/查看提交记录与评测反馈（隐藏用例不泄露 I/O）。 |
| 提交与截止时间 | 提交写入 `Submission`（`PENDING`），向 Redis 队列投递任务；**截止时间取自所属 LabSet.dueAt**，过期拒绝提交。 |
| 异步评测 | judge-worker 消费队列：逐用例 `spawn` 运行 node/python，超时杀进程；按权重计分；状态 `ACCEPTED` / `WRONG_ANSWER` / `ERROR` / `TIMEOUT`；无测试用例时默认满分说明。 |
| 教师统计 | 如 `teacher-metrics`：选课人数、提交数、AC 人数、各用例通过情况等（供教学分析）。 |
| 实验集进度与罚时 | `lab-set-penalty.ts`：ICPC 风格罚时（自实验集创建起算 + 错罚分钟数）；接口在 `lab-sets` 的学生进度/教师视图中返回 `penaltyRule` 与分析结果。 |

**实现要点**：`backend/src/routes/lab-sets.ts`、`backend/src/routes/labs.ts`、`backend/src/routes/lab-files.ts`、`backend/src/lib/queue.ts`、`backend/src/lib/lab-set-penalty.ts`；`judge-worker/src/worker.ts`、`judge-worker/src/runner.ts`；前端 `LabSetHub.tsx`、`LabSetManage.tsx`、`LabProblemCreateModal.tsx`、`Lab.tsx`。

---

### 2.4 讨论区

| 功能 | 详细描述 |
|------|----------|
| 课程级讨论 | 同一课程下 `labId` 为空的帖子；选课学生或教师可读写列表。 |
| 题目级讨论 | `GET/POST /labs/:labId/discussions`；需能访问该实验（选课或教师/管理员）。 |

**实现要点**：`backend/src/routes/discussions.ts`；`DiscussionPost` 模型含 `labId`；前端在课程页/实验相关页发帖与列表。

---

### 2.5 AI 辅助

| 功能 | 详细描述 |
|------|----------|
| 实验 AI 答疑 | `POST /labs/:labId/ai-help`：支持单轮 `question` 或多轮 `messages`；系统提示约束（不泄露隐藏用例、不输出完整题解等）。配置 `OPENAI_API_KEY` / `DEEPSEEK_API_KEY` 与 `OPENAI_BASE_URL`、`OPENAI_MODEL` 时走 OpenAI 兼容 Chat；否则返回**本地模板**降级说明。独立**每分钟每 IP** 限流（`aiRouteRateLimitMaxPerMinute`）。 |
| 作业 AI 建议 | 作业批改流中调用（如 `homework/submissions/:id/ai-suggest`），结合 `homework-ai-config`（支持 Ollama 等）；未配置时启发式。 |

**实现要点**：`backend/src/routes/ai-help.ts`、`backend/src/lib/openai-chat.ts`、`backend/src/lib/homework-ai-config.js`、`backend/src/routes/homework.ts`。

---

### 2.6 作业全流程

| 功能 | 详细描述 |
|------|----------|
| 作业 CRUD / 发布 | 教师创建作业（标题、说明、截止时间、**指定班级** `targetClassId`、是否立即发布）；发布/撤回。 |
| 学生提交 | 同一学生对同一作业 upsert 一条 `HomeworkSubmission`；重复提交可重置批改状态（以路由实现为准）。 |
| 教师批改 | 打分、反馈、发布成绩；批改台拉取全班提交列表；支持 **AI 建议**（预览或应用）。 |
| 作业问答 | `HomeworkQuestion`：学生对作业提问，教师可回答（模型支持 `answeredBy` 等）。 |

**实现要点**：`backend/src/routes/homework.ts`；前端 `MyHomework.tsx`、`TeachingHomeworkList.tsx`、`HomeworkTeacherReview.tsx`、`CourseDetail.tsx` 内嵌作业区。

---

### 2.7 成绩与导出

| 功能 | 详细描述 |
|------|----------|
| 成绩册（教师） | 按课程拉取全班：每位学生按 **实验集** 聚合（集内各题最高分均分 → 多集再均分得到实验总均分）、作业均分、加权总评、排名；**分数段人数分布**（低于 60、60–69、…、90 及以上及无总评人数）。 |
| 成绩册 CSV | 教师导出 CSV（UTF-8 BOM），列含各题、各实验集均分、作业、总评、排名等。 |
| 学生成绩总览 | `GET /grades/me`：每门课一条摘要（排名、班级人数、总评、权重）。 |
| 学生导出 CSV | `GET /grades/me/export.csv` 个人成绩册。 |
| 总评权重配置 | `GET/PATCH /courses/:courseId/grading-config`，要求 `labWeight + homeworkWeight = 1`。 |

**实现要点**：`backend/src/routes/grades.ts`（`loadGradebook`、`toCsv`、`buildMyGradebookCsv`）；前端 `Gradebook.tsx` 等。

---

### 2.8 教学台与个人信息

| 功能 | 详细描述 |
|------|----------|
| 教学台 | 教师入口聚合所授课程与快捷操作（与路由 `/teaching` 等一致）。 |
| 个人中心 | `/profile` 登录后维护资料等。 |

**实现要点**：`frontend/src/pages/Teaching.tsx`、`Profile.tsx`。

---

### 2.9 运维、安全与工程化

| 功能 | 详细描述 |
|------|----------|
| 健康检查 | `GET /health/live`（存活）、`/health/ready`（DB + Redis）。 |
| 全局中间件 | CORS、Helmet（CSP 关闭便于本地开发）、multipart 上限、**全局限流**、请求体大小上限、`X-Request-ID`。 |
| 根脚本 | `npm run dev` 同时起前后端；`dev:full` 含 judge-worker；`db:migrate`、`db:seed`、`perf:smoke` 冒烟压测。 |

**实现要点**：`backend/src/index.ts`、`backend/src/routes/health.ts`、`backend/src/lib/config.ts`；`scripts/smoke-load.mjs`。

---

## 三、对应实现方式（架构与模块映射）

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 前端 | React 19、React Router 7、Vite 6、Axios、`@monaco-editor/react`、`react-markdown` | `baseURL: '/api'`，Bearer 来自 `localStorage`。 |
| 后端 | Fastify、@fastify/cors / helmet / multipart / rate-limit、Zod 校验、Prisma | 路由按域拆分注册于 `index.ts`。 |
| 数据库 | PostgreSQL + Prisma Migrate | 模型见 `schema.prisma`。 |
| 评测 | BullMQ + Redis；worker 独立进程 | API `getJudgeQueue().add`；worker `concurrency: 4` 可配置。 |
| 代码执行评测 | Node / Python3 子进程 + 临时目录 | `judge-worker/src/runner.ts`，超时 `JUDGE_TIMEOUT_MS`（默认 8000ms）。 |

**典型请求链（实验提交）**：前端 `POST /api/labs/:id/submit` → 后端写 `Submission` 并入队 → worker 拉取 job → 跑用例 → 更新 `status/score/resultJson` → 前端轮询 `GET /submissions/:id` 或反馈接口。

---

## 四、性能分析

### 4.1 优势与设计取向

- **评测与 Web 解耦**：高峰提交时主要压力在 Redis 队列与 worker，可横向扩展 `judge-worker`（见 `RUN.txt` Docker 扩容示例）。
- **无状态 API + JWT**：便于多实例 API 后挂负载均衡。
- **限流与请求体限制**：减轻恶意大包与刷接口；AI 路由另有更严的 per-IP 限流。

### 4.2 潜在瓶颈与复杂度

| 点 | 说明 |
|----|------|
| 成绩册 `loadGradebook` | 对每门课一次查询 enrollments + submissions + homeworkSubmissions，再在内存中按学生聚合；班级人数很大时 CPU 与响应时间上升，可考虑缓存或分页导出。 |
| 评测子进程 | 每用例一次进程启动，高并发时宿主机进程与 IO 压力大；`worker` 默认 `concurrency: 4` 限制并行度。 |
| 沙箱安全 | 当前为教学演示级（本机 node/python）；**非强隔离**，公网多租户需换容器级沙箱（`RUN.txt` 安全提示）。 |
| 数据库连接 | Prisma 默认连接池；多实例 API 时注意 `DATABASE_URL` 连接数上限。 |

### 4.3 参考吞吐探测

根目录 `npm run perf:smoke` 对 `health/ready` 做并发 fetch，输出成功数与 **req/s**；可通过 `CONCURRENCY`、`REQUESTS`、`TARGET_URL` 环境变量调整（见 `scripts/smoke-load.mjs` 与 `RUN.txt`）。

---

## 五、测试与验收规范（面向测试/验收人）

以下将「测什么、怎么判定通过、性能如何量」写成可执行的验收逻辑。用例编号便于缺陷单与回归列表引用。

### 5.1 术语与范围

| 术语 | 含义 |
|------|------|
| **SUT** | 被测系统：本仓库前后端 +（按需）judge-worker。 |
| **P0/P1/P2** | P0 阻塞发布；P1 重要功能缺陷；P2 一般/体验。 |
| **证据** | 截图、录屏、响应体片段、HTTP 状态码、导出文件、后端日志中的 `requestId`。 |

**不在默认范围**：真实邮件发送、生产级代码沙箱强度、第三方 LLM 内容正确性（仅测「有响应/降级/限流」）。

### 5.2 准入条件与环境矩阵

**基础环境**（与 `RUN.txt` 一致）：Node 20+、PostgreSQL、Redis、`backend/.env` 已配置、`prisma migrate deploy` + `db:seed` 已完成。

**首次搭建步骤（验收环境搭建）**：根目录 `npm install` → `npm run install:all` → 配置 `backend/.env` → `cd backend && npx prisma migrate deploy && npm run db:seed` → 根目录 `npm run dev`（全量含评测再加 `npm run dev:worker` 或使用 `npm run dev:full`）→ 浏览器 `http://localhost:5173`。

| 矩阵 ID | 后端 | Redis | judge-worker | 用途 |
|---------|------|-------|--------------|------|
| **A** | 运行 | 运行 | 不运行 | 除「提交评测终态」外的大部分功能、健康检查、列表类接口。 |
| **B** | 运行 | 运行 | 运行 | 全量含编程实验评测、队列积压观察。 |
| **C** | 运行 | 停止或错误 URL | 任意 | 负向：`/health/ready` 应非 200；实验提交可仍入队但 job 失败（用于运维验收）。 |

**浏览器**：推荐 Chromium 系；分辨率 1280×720 及以上。  
**账号**：种子脚本输出的教师 / 学生 / 管理员（见 `RUN.txt`）；验收中应固定使用同一批账号便于复现。

### 5.3 功能测评用例集

**书写约定**：每用例「预期」全部满足即 **Pass**；任一不满足记 **Fail** 并挂缺陷（注明用例编号）。

#### 5.3.1 健康与网关（矩阵 A）

| 编号 | 优先级 | 前置 | 步骤 | 预期 | 证据建议 |
|------|--------|------|------|------|----------|
| TC-H-01 | P0 | 后端已启动 | `GET http://127.0.0.1:3000/health/live` | HTTP 200，JSON 含 `ok: true`，`type` 为 `live` | 响应文本或抓包 |
| TC-H-02 | P0 | 矩阵 A | `GET http://127.0.0.1:3000/health/ready` | HTTP 200，`ok: true`；响应头含 `X-Request-ID` | 同上 |
| TC-H-03 | P1 | 停 Redis 或改错 `REDIS_URL` 后重启 API | 再次 `GET /health/ready` | HTTP 503 或 `ok: false`（与实现一致），不得 500 无信息 | 响应体 |
| TC-H-04 | P1 | 前端 `npm run dev` | 浏览器打开 `http://localhost:5173`，打开开发者工具 Network | 页面可加载；后续 XHR 指向 `/api/...` 且非连续失败 | 截图 |

#### 5.3.2 认证与权限（矩阵 A）

| 编号 | 优先级 | 前置 | 步骤 | 预期 | 证据建议 |
|------|--------|------|------|------|----------|
| TC-A-01 | P0 | 无 | 注册新邮箱用户，再登录 | 返回 token；`/auth/me` 可拉取本人信息 | 脱敏响应 |
| TC-A-02 | P0 | 学生 token | 访问仅教师路由（如直接 URL 打开课程管理页） | 重定向或禁止，不得出现教师专属写操作成功 | 截图 |
| TC-A-03 | P0 | 未登录 | 访问需登录页（如 `/courses/:id/labs/:labId`） | 跳转登录 | 截图 |
| TC-A-04 | P1 | 已登录 | 修改资料、修改密码（旧密码正确） | 成功；新密码可登录 | 简要记录 |
| TC-A-05 | P1 | 演示环境 | 调用忘记密码接口，使用返回的 `resetTokenHint` 重置 | 新密码登录成功 | 注意勿在生产依赖 hint |
| TC-A-06 | P0 | 管理员账号 | 打开 `/admin/users` 或 `GET /admin/users` | 列表有数据且仅管理员可访问 | 截图 / 401 非管理员 |

#### 5.3.3 课程、选课、资料（矩阵 A）

| 编号 | 优先级 | 前置 | 步骤 | 预期 | 证据建议 |
|------|--------|------|------|------|----------|
| TC-C-01 | P0 | 学生已登录 | 课程列表选一门已发布课程进入详情并选课 | 选课成功，详情页展示一致 | 截图 |
| TC-C-02 | P1 | 教师 | 课程管理修改标题、权重（和为 1）、保存 | 再次进入详情或成绩配置与之一致 | 前后对比 |
| TC-C-03 | P1 | 教师 | 上传课程资料；学生已选课 | 学生可见列表且可下载 | 下载文件哈希或大小 |
| TC-C-04 | P1 | 未发布课程 + 非教师学生 | 学生访问该课 | 列表不可见或详情拒绝（与产品规则一致） | 截图或 403 |

#### 5.3.4 实验集、题目、附件、讨论（矩阵 A/B）

| 编号 | 优先级 | 前置 | 步骤 | 预期 | 证据建议 |
|------|--------|------|------|------|----------|
| TC-L-01 | P0 | 教师 | 创建实验集、进入管理、新建题目（JS 或 Python）、添加至少 1 个公开用例 | 学生侧可见实验集与题目入口 | 截图 |
| TC-L-02 | P0 | 矩阵 B，学生已选课 | 打开单题实验页，提交能通过全部用例的代码 | `Submission` 由 `PENDING` 变为 `ACCEPTED`（或等价终态），分数合理 | 提交记录截图 |
| TC-L-03 | P1 | 矩阵 B | 故意提交错误代码 | `WRONG_ANSWER` 或 `ERROR`，反馈中**不展示** hidden 用例的输入输出 | 响应/页面 |
| TC-L-04 | P0 | 教师设置 LabSet `dueAt` 为过去 | 学生再提交 | HTTP 403，文案含截止相关提示 | Network 面板 |
| TC-L-05 | P1 | 教师 | 删除空实验集；再创建有题集并带 `force` 删除 | 行为与确认弹窗符合设计，数据不可恢复提示清晰 | 录屏 |
| TC-L-06 | P1 | 学生 | 课程讨论发帖；进入某题讨论发帖 | 列表可见且顺序合理 | 截图 |
| TC-L-07 | P1 | 未选课学生 | 访问他人实验讨论接口 | 403 | 抓包 |

#### 5.3.5 AI 与限流（矩阵 A）

| 编号 | 优先级 | 前置 | 步骤 | 预期 | 证据建议 |
|------|--------|------|------|------|----------|
| TC-I-01 | P1 | 未配置 API Key | `POST /labs/:labId/ai-help` 带 `question` | 返回 `source: template` 或等价降级，有中文提示文案 | 响应体 |
| TC-I-02 | P1 | 已配置 Key（若有） | 同一题连续多轮 `messages` | 有 assistant 回复；不出现隐藏用例原文 | 脱敏响应 |
| TC-I-03 | P2 | 任意 | 1 分钟内对 ai-help 发起超过 `AI_RATE_LIMIT_MAX` 次 | 出现 429 或路由层限流错误（与实现一致） | 状态码统计 |

#### 5.3.6 作业与批改（矩阵 A）

| 编号 | 优先级 | 前置 | 步骤 | 预期 | 证据建议 |
|------|--------|------|------|------|----------|
| TC-HW-01 | P0 | 教师 | 创建作业，指定班级/全课程，发布 | 目标学生可见并可提交 | 截图 |
| TC-HW-02 | P0 | 学生 | 填写内容提交作业 | 成功；教师批改台可见 | 教师端截图 |
| TC-HW-03 | P0 | 教师 | 批改打分、发布成绩 | 学生侧看到分数或「待发布」状态符合规则 | 双端截图 |
| TC-HW-04 | P1 | 教师 | 对某提交请求 AI 建议（预览不应用） | 返回建议结构且无 500 | 响应片段 |

#### 5.3.7 成绩册与导出（矩阵 A）

| 编号 | 优先级 | 前置 | 步骤 | 预期 | 证据建议 |
|------|--------|------|------|------|----------|
| TC-G-01 | P0 | 教师，班内有实验与作业数据 | 打开成绩册页 | 每行学生有实验集维度、总评、排名；分布段人数之和与口径说明一致 | 截图 |
| TC-G-02 | P0 | 同上 | 导出 CSV | UTF-8 可读，Excel 打开不乱码；含表头与学生行 | 附件 |
| TC-G-03 | P0 | 学生 | 「我的成绩」与导出个人 CSV | 与教师端该生总评一致（同一公式） | 数值对比表 |

#### 5.3.8 实验集罚时与进度（矩阵 A，有提交数据更佳）

| 编号 | 优先级 | 前置 | 步骤 | 预期 | 证据建议 |
|------|--------|------|------|------|----------|
| TC-P-01 | P1 | 已有多次 WA 后 AC 的提交 | 打开实验集学生进度/教师视图中含 `penaltyRule` 的接口返回 | 字段齐全；总罚时与规则说明可人工核对抽样 | JSON 片段 |

### 5.4 权限与异常（安全验收抽样）

| 编号 | 步骤 | 预期 |
|------|------|------|
| TC-S-01 | 篡改他人 `submissionId` 拉取详情 | 403 或 404，不得泄露他人代码 |
| TC-S-02 | 无 token 调 `POST /labs/:id/submit` | 401 |
| TC-S-03 | 超大请求体（超过 `BODY_LIMIT_BYTES`） | 4xx，服务不崩溃 |
| TC-S-04 | 同一登录会话连续失败登录（注册接口同理） | 触发 rate-limit 后受限（与配置一致） |

### 5.5 性能测评逻辑

#### 5.5.1 目标与指标

| 指标 | 含义 | 采集方式 |
|------|------|----------|
| **就绪 QPS** | API 在 DB+Redis 正常时读健康检查吞吐 | `npm run perf:smoke` |
| **成功率** | 压测脚本中 HTTP 2xx 占比 | 脚本输出 `Success` |
| **P95 延迟（可选）** | 关键读接口 | 外部工具如 hey/wrk 或自定义脚本（仓库未内置） |

#### 5.5.2 标准场景定义

| 场景 ID | 环境 | 命令（在项目根目录） | 记录项 |
|---------|------|----------------------|--------|
| PERF-01 基线 | 仅 API+DB+Redis，无 worker | `npm run perf:smoke` | `Throughput req/s`、`Success/N` |
| PERF-02 加压 | 同上 | CMD：`set CONCURRENCY=100&& set REQUESTS=2000&& npm run perf:smoke`；PowerShell：`$env:CONCURRENCY=100; $env:REQUESTS=2000; npm run perf:smoke` | 同上；记录 CPU/内存峰值（任务管理器） |
| PERF-03 目标 URL | 指向部署环境 | 设置 `TARGET_URL` 后执行 `npm run perf:smoke`（同上，CMD `set` / PowerShell `$env:TARGET_URL=...`） | 网络延迟下 QPS 下降可接受范围由项目约定 |

#### 5.5.3 通过准则（建议值，可由项目经理签章调整）

| 场景 | 建议通过线 | 说明 |
|------|------------|------|
| PERF-01 | `Success == Requests`，吞吐大于项目约定的最低 QPS（如单机 dev 环境 **≥ 50 req/s** 仅作参考） | dev 机差异大，**以「无失败 + 无异常日志」为主** |
| PERF-02 | 成功率 **≥ 99%**；若明显低于则查 DB 连接数、Redis、宿主机换页 | 失败多为网络或端口不可达 |
| 成绩册页 | 教师打开 **≤ 3s**（演示数据量下）为体验参考；超大数据集单列 **性能基线** | 超时可记 P2 优化项而非直接 Fail |

#### 5.5.4 评测链性能（矩阵 B）

| 步骤 | 记录 | 建议 |
|------|------|------|
| 连续 N 次合法提交（N≤worker 并发×2） | 从提交到终态的耗时分布 | 若大量 `PENDING` 积压，检查 worker 数与 `JUDGE_TIMEOUT_MS` |
| 单题多用例 | 总耗时上限经验值 **≤ 用例数 × 超时 + 队列等待** | 超阈记性能缺陷 |

### 5.6 回归策略与发布闸门

1. **Smoke（每次构建）**：TC-H-01、TC-H-02、TC-A-03、TC-C-01（或固定种子课）、TC-L-02（矩阵 B）。  
2. **全量回归（发版前）**：本节全部 P0 + 抽样 P1。  
3. **闸门**：P0 全部 Pass；P1 Fail 数 ≤ 0 或已评估带风险上线说明。

### 5.7 测试执行记录（模板）

复制下表到测试管理工具或 Excel：

| 用例编号 | 执行人 | 日期 | 环境矩阵 | 结果 Pass/Fail/Blocked | 缺陷单号 | 备注 |
|----------|--------|------|----------|------------------------|----------|------|
| TC-H-01 | | | A | | | |

### 5.8 常见问题排查（运维联调）

| 现象 | 排查方向 |
|------|----------|
| 提交一直 PENDING | Redis、judge-worker、队列名 `judge-submissions` |
| 前端 404 API | Vite `/api` 代理、后端端口 3000 |
| 数据库错误 | `migrate deploy`、`DATABASE_URL` |
| AI 无云端回复 | `.env` Key/BaseUrl；无 Key 时应仍返回模板降级 |
| `/health/ready` 503 | PostgreSQL 与 Redis 连通性 |

---

## 六、文档维护说明

若后续增加路由或页面，请同步更新本文「功能列表」「实现要点」及**第五节用例表**（新增用例编号与矩阵说明）。

---

*文档生成依据：仓库内源码与 `RUN.txt`，不保证与未提交的本地修改一致。*
