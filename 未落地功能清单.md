# 未落地功能清单（对照当前代码库）

> 本文档供组内分工使用。依据截至目前的 **仓库实际代码**（`backend/src`、`frontend/src`、`judge-worker`、`backend/prisma/schema.prisma`）与 `已实现功能.md` 对照整理，**不夸大已实现范围**。

---

## 1. 说明

### 1.1 已具备的基础（避免重复分工）

以下在代码中已有对应实现，**不列入“未落地”**（细节见 `已实现功能.md`）：

- 课程 CRUD、选课、班级、课程资料上传、知识图谱 JSON、分类筛选；
- 实验（Monaco）、测试用例、BullMQ 异步评测、作业全流程、成绩册与 CSV 导出、权重配置；
- 用户注册登录 JWT、个人资料与密码、演示版邮箱验证/重置（返回 token 提示）、管理员用户列表；
- 课程讨论区（发帖列表 + 发帖，无楼层）；
- 健康检查、限流、Helmet、请求体限制等基础非功能项。

### 1.2 与《需求规格说明书》（计划口径）的差异

当前仓库技术栈为 **React + Vite + Fastify + Prisma + PostgreSQL + Redis（BullMQ）**，与文档中计划的 **Vue 3 + Spring Boot + MySQL + RabbitMQ + 宝塔** 不一致。若课程要求按计划书技术栈交付，则 **整体迁移或双轨实现** 属于未在现有代码中落地的工作，需单独排期。

---

## 2. 按模块：当前代码中未落地或明显不完整的能力

### 2.1 课程与教学内容组织

| 项 | 现状（诚实描述） | 建议分工方向 |
|----|------------------|--------------|
| **章节/大纲结构** | 仅有 `Course` + `CourseMaterial`（扁平资料），无章节、无排序大纲、无“课节”实体。 | 数据模型 + 前后端 CRUD + 资料挂章节 |
| **课程公告（教师发布、全员可见）** | 无独立公告模型与接口；`DiscussionPost` 为讨论帖，语义与“公告”不同。 | 公告表 + 教师发布 + 学生只读展示 |
| **讨论区进阶** | 仅 `GET/POST` 发帖，无回复楼层、无点赞、无教师置顶/锁帖。 | 回复模型或嵌套评论 + 前端展示 |

### 2.2 作业与考试形态

| 项 | 现状 | 建议分工方向 |
|----|------|--------------|
| **作业附件提交** | `HomeworkSubmission.content` 为文本字段；无作业维度附件上传/下载接口。 | multipart + 存储路径 + 下载鉴权 |
| **客观题/题库** | 无题目库、无选择题/填空自动判分模型。 | 题库表、组卷、判分逻辑（若需要） |
| **成绩导出 Excel** | 成绩导出为 **CSV**（见成绩相关路由与前端），无 xlsx 生成。 | 选用库导出 xlsx 或保留 CSV 并改文档口径 |

### 2.3 通知与消息

| 项 | 现状 | 建议分工方向 |
|----|------|--------------|
| **真实邮件发送** | 验证/重置流程为演示向（接口返回 token 类提示），**无 SMTP/第三方邮件 SDK**。 | 接入邮件服务 + 模板 + 异步发送 |
| **站内消息中心** | 无消息表、无统一通知列表、无已读未读。 | 消息模型 + API + 前端消息页 |
| **RabbitMQ** | 仓库使用 **BullMQ + Redis** 做评测队列，**无 RabbitMQ** 集成。 | 若课程硬性要求 RabbitMQ，需评估迁移或抽象队列层 |

### 2.4 用户与权限管理

| 项 | 现状 | 建议分工方向 |
|----|------|--------------|
| **管理员操作** | 仅有 `GET /admin/users` 列表；**无**后台改角色、禁用账号、重置密码、删除用户等。 | 管理 API + 审计/二次确认 + 前端 |
| **注册角色** | 注册接口允许自选 `STUDENT`/`TEACHER`（见 `auth` 路由），无审核机制。 | 若需管控：默认学生 + 教师申请审核 |

### 2.5 自动评测与安全

| 项 | 现状 | 建议分工方向 |
|----|------|--------------|
| **语言支持** | 创建实验时 `language` 仅枚举 **`javascript` / `python`**；Worker 用本机 `node`/`python3` 执行。 | 扩展语言需 runner + 镜像/依赖策略 |
| **沙箱隔离强度** | 临时目录 + 子进程，**非**完整容器化强隔离沙箱。 | Docker/受限运行时、资源限额、网络禁用 |
| **查重/相似度** | 若计划书要求“作业查重”，当前相似度接口针对 **实验提交**（见 `labs` 相关实现），作业文本侧 **未**单独实现。 | 作业文本指纹/第三方查重（按预算） |

### 2.6 运维、测试与文档工程

| 项 | 现状 | 建议分工方向 |
|----|------|--------------|
| **自动化测试** | 仓库内 **未发现** `*.test.ts` / `*.spec.ts` 等测试文件。 | 后端单测、API 集成测、前端关键流 E2E（择一起步） |
| **OpenAPI 文档** | 无自动生成/手写的 OpenAPI 仓库产物（接口分散在路由文件中）。 | fastify-swagger 或导出 openapi.yaml |
| **定时备份脚本** | 代码库中无数据库备份/恢复脚本（若算交付物需补）。 | pg_dump 脚本 + 文档说明 |
| **计划部署方式（宝塔/腾讯云）** | 存在 Docker Compose 等配置，**与“宝塔面板部署说明”不是同一套**，且未在代码中体现一键宝塔流程。 | 按学校要求补《部署文档》与实操步骤 |

### 2.7 前端体验（相对计划书表述）

| 项 | 现状 | 建议分工方向 |
|----|------|--------------|
| **移动端专项** | 未做专门的移动适配与测试；以桌面浏览器为主。 | 响应式布局抽测 + 关键页适配 |
| **教师“数据看板”** | 有成绩册统计等能力，**无**独立大屏式看板（图表、多课程汇总等需新增页面与接口）。 | 看板需求裁剪 + 图表库 |

---

## 3. 组内分工建议（可按人认领）

以下为 **与当前缺口对齐** 的拆分示例，组长可按成员意愿调整：

1. **教学内容**：章节/大纲 + 课程公告 +（可选）讨论回复。  
2. **作业增强**：作业附件 +（可选）题库/客观题。  
3. **通知与集成**：真实邮件 + 站内消息；（若课程要求）RabbitMQ 与现有队列关系说明或迁移方案。  
4. **管理后台**：管理员用户维护能力（改角色、禁用等）与安全流程。  
5. **评测与安全**：沙箱强化或语言扩展（与工期匹配二选一优先）。  
6. **质量与交付**：自动化测试、OpenAPI、备份脚本、部署文档（宝塔/云主机或 Docker 二选一定稿）。

---

## 4. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-05-06 | 首版，对照本仓库代码整理未落地项 |
