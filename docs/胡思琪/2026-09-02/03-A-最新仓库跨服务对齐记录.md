# A：最新仓库跨服务对齐记录

- 复核日期：2026-09-02
- 范围：A 的 `course-service`、A 的依赖清单，以及 B/C/Gateway 已提交代码的只读核对。
- 原则：不修改 B、C、D、E 代码；跨服务不可用时 A 继续 fail-closed 或显式降级，不伪造数据。

## 已对齐

| 调用方向 | 结果 | 证据 |
| --- | --- | --- |
| C → A：课程名单 | 已对齐 | A 返回 `items/total/page/pageSize` 与 `classId`，匹配 C `parseEnrollmentRoster`。 |
| C → A：批量通知 | 已对齐 | A 接收 `userIds[]` 和 `Idempotency-Key`；C 同时发送请求头与兼容 body 字段，A 只以请求头去重。 |
| C → A：用户、课程、权限、班级 | A 已实现 | A 提供用户 `status`、课程教师/发布字段、权限 `classIds` 和班级列表；C 当前客户端尚未切换。 |
| A → C：作业 Dashboard | 已对齐 | A 调用 C 已实现的 `GET /internal/courses/:courseId/homework-summary`。 |
| Gateway → A：路由规则 | 静态规则已对齐 | `/api/auth`、`/api/enrollment`、`/api/dashboard` 等归 A；`/internal/**` 不经网关。 |

## 未对齐

| 调用方向 | 差异 | 影响 | 责任人 |
| --- | --- | --- | --- |
| A/C → B：实验成绩 | C 约定 `GET /internal/courses/:courseId/lab-gradebook`；B 实际只提供单人和 `POST /lab-grades/batch` | C 的总评和 A 的 Lab Dashboard 无法按冻结契约获取结果 | B |
| A/C → B：内部鉴权 | 约定 `x-internal-service-token` / 共享 `INTERNAL_SERVICE_TOKEN`；B 实际使用 `x-internal-token` / `INTERNAL_API_TOKEN` | 即使 URL 对齐也会得到 401，且无法安全共用配置 | B |
| A/C → B：服务端口 | 约定 Lab 为 `:3003`；B 默认 `:3001` | 与 A 默认端口冲突，Compose/Gateway 无法可靠路由 | B |
| A/C → B：错误体 | 约定 `{ code,message,requestId }`；B 内部接口返回 `{ error }` | 调用方无法按统一错误码、请求追踪做降级记录 | B |

## 本轮验证

| 项目 | 结果 |
| --- | --- |
| `course-service npm run build` | 通过 |
| `course-service npm run test` | 5/5 通过 |
| `api-gateway npm run build` | 未验证：本机 `api-gateway` 缺少已安装依赖，`tsc` 不存在；未修改 C 代码或依赖 |

详细待办见 [04-A-仍需处理事项清单.md](04-A-仍需处理事项清单.md)。
