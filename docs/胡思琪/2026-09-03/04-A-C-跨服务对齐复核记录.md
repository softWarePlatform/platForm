# A / C 跨服务内部 API 对齐复核记录

- 复核日期：2026-09-03
- 输入：C 的 `01-C-跨服务内部API对齐.md` 与最新分支的 `homework-grade-service`、`api-gateway` 代码。
- 范围：仅 A 的课程服务、配置和文档。

## 已对齐并实现

| C 的需求 | A 的实现 | 验证 |
| --- | --- | --- |
| 课程名单分页、班级过滤、扁平用户项 | `GET /internal/courses/:courseId/enrollments?page&pageSize&classId?` 返回 `courseId/items/total/page/pageSize` | A API 冒烟测试通过 |
| 批量成绩发布通知 | `POST /internal/notifications` 接受 `userIds[]`，需要请求头 `Idempotency-Key`，响应含 `created/deduped` | A API 冒烟测试验证首次创建与重复请求 |
| 用户/课程/权限/班级 | 返回 `status: ACTIVE`、课程字段、服务端权限判断及 `classIds` | A API 冒烟测试验证内部鉴权与选课权限 |
| Dashboard 作业摘要 | A 调用 C 已实现的 `/internal/courses/:courseId/homework-summary` | Unit/契约测试验证请求路径和成功响应解析 |
| 请求追踪与内部鉴权 | 使用共享 `x-internal-service-token` 与 `x-request-id`；内部错误体含 `code/message/requestId` | A API 冒烟测试验证无令牌为 401 |

## 仍需对齐或执行

| 项目 | 负责人 | 状态与下一步 |
| --- | --- | --- |
| 真实 A↔C 进程联调 | A + C | C 需启动 `:3002` 并设置与 A 一致的 `INTERNAL_SERVICE_TOKEN`；随后以真实课程 ID 调用 A Dashboard，验证作业摘要显示为 `OK`。 |
| C 切换 P1 内部权限客户端 | C | 当前 `course-client.ts` 仍用公开 `/auth/me`、`/courses/:id`、`/enrollment/catalog` 做权限；A 的内部端点已准备好。 |
| Gateway 公开路由回归 | A + C | 最新网关路由规则已存在，需启动 `:3081` 验证 `/api/enrollment/**`、`/api/dashboard/**` 透传到 A。 |
| Lab Dashboard 摘要 | B | B 尚未提供 A 可用的 Dashboard 摘要契约；A 继续返回 `UNAVAILABLE`，不以零值替代。 |

## 验收命令

在 `course-service` 目录执行：

```text
npm run db:migrate
npm run db:seed
npm run test
npm run test:api
```

本次结果：迁移无待执行项，Unit/契约测试 5/5 通过，API 冒烟测试通过。`db:seed` 仅限本地演示数据库，会清空 `course_service` 数据。
