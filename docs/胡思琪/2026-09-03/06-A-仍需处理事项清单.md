# A：跨成员仍需处理事项清单

- 更新日期：2026-09-03
- 范围：当前代码已对齐后的真实联调、追踪和部署事项。A 不修改 B、C、Gateway 或集群代码。

| 优先级 | 事项 | 负责人 | 当前依据 | 完成标准 |
| --- | --- | --- | --- | --- |
| P0 | A、B、C 三服务 Dashboard 真联调 | A + B + C | A 已按冻结接口调用 `:3002` 与 `:3003`，但本轮只启动了 A。 | 使用同一 `INTERNAL_SERVICE_TOKEN`，以一个真实课程调用 A `/dashboard/me`；`dependencies.homework.status` 与 `dependencies.lab.status` 均为 `OK`，无分时保持 `null` 而非 0。 |
| P1 | B 补 gradebook → A 名单请求追踪 | B | `internal-lab-grades.ts` 调用 `fetchCourseUserIds(courseId)`，该函数未接收并传递 `req.id`。 | B 将 gradebook 的 `req.id` 传到 `fetchCourseUserIds`/`fetchCourseRoster`，A 日志与 B 请求日志使用同一个 `x-request-id`。 |
| P1 | C 补内部 Course 调用请求追踪 | C | C 的课程、权限、名单内部请求使用共享令牌，但只有通知路径可选传 `x-request-id`。 | 从 C 入口请求到 A 的内部调用全部携带同一请求 ID；超时和 4xx/5xx 记录可关联。 |
| P1 | C 修复超过 200 人的名单读取 | C | C 当前只请求 A 名单的第 1 页 `pageSize=200`；A 已返回 `total` 和分页字段。 | C 根据 `total` 迭代页面；201 名学生的测试不丢失最后一人。 |
| P1 | Gateway 实际公开路由回归 | C / 环境负责人 | 静态规则正确，但本轮未启动 `:3081`。 | `/api/enrollment/**`、`/api/dashboard/**` 能到 A；`/internal/**` 不暴露；A 不可用时返回可追踪的 502。 |
| P2 | Lab 数据边界和迁移方案冻结 | A + B + C + D | B 已迁入大部分领域，但独立数据迁移、回滚和最终所有权仍待团队决定。 | 书面确认 Lab 独立数据范围、迁移/回滚步骤和跨服务仅用 API 的约束；A 仅参与边界确认。 |
| P2 | Kubernetes 与全服务故障验证 | D + E | 本轮只有 A 本地测试；无集群及跨服务故障证据。 | 完成健康探针、共享 Secret、正常链路、错误令牌、B/C 停止后的 `UNAVAILABLE/502` 与恢复证据。 |

## A 已可交付，无需等待他人继续编码

- 课程、用户、课程权限、班级/名单、管理员和通知内部接口；
- 批量用户读取与通知幂等；
- A Dashboard 对 Homework 与 Lab 的 HTTP 聚合和不将未知成绩写为 0 的降级；
- 构建、5 条单元/契约测试及本地 API 冒烟。

下一步只有在 B/C 可启动并提供共享令牌，或负责人要求 A 配合 Gateway/集群联调时，才需要继续修改或补充 A 的证据。
