# A 工作简报：8 月 31 日与 9 月 1 日

## 昨日完成（2026-08-31）

完成 `course-service` 首日拆分：建立独立 Node.js/Fastify/TypeScript 工程，提供健康检查和课程目录/详情核心 API；完成课程域路由归属与数据边界审计，明确 Dashboard 不得直连作业、实验和评测表。发现宿主机 `5433` 被非项目 PostgreSQL 占用后，已将项目 Docker PostgreSQL 端口调整为 `55432`，现有单体演示数据保留。

## 今日完成（2026-09-01）

完成课程服务独立数据库 `course_service`、首个 Prisma 迁移和专属种子数据。实现认证、课程创建/编辑/发布、选课/退课/候补/冲突校验、公告已读与通知、资料上传/收藏/下载，以及管理员选课阶段、用户和审计查询。独立 API 冒烟、路由测试、构建和迁移部署均通过；课程服务不包含作业、实验、练习、讨论、评测和成绩表。

## 明日重点（2026-09-02）

实现并冻结对 B/C 的内部用户、课程、权限、名单和通知接口；将 Dashboard 改为通过 homework/lab 的内部 HTTP 汇总接口获取数据，并补齐超时与部分结果降级测试。
