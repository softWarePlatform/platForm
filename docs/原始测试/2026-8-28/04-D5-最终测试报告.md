# D5 最终测试报告

## 执行摘要

教学平台以UC-01～UC-10为验收范围，建立了需求、三层设计、代码模块与四层自动化测试之间的追溯关系。本机 `npm test` 已一次性执行单元、DAO、API和E2E测试，72/72通过；同一套10条E2E在Docker Desktop Kubernetes再次独立回归，10/10通过；完整生产构建通过。

## 测试结果

| 测试类型 | 总数 | 通过 | 失败 | 运行环境 | 命令 |
|---|---:|---:|---:|---|---|
| 单元测试 | 43 | 43 | 0 | 本机Node.js | `npm run test:unit` |
| DAO集成测试 | 4 | 4 | 0 | 本机PostgreSQL | `npm run test:dao` |
| API测试 | 15 | 15 | 0 | 本机API | `npm run test:api` |
| E2E测试 | 10 | 10 | 0 | 本机API/数据库 | `npm run test:e2e` |
| 合计 | 72 | 72 | 0 | 本机 | `npm test` |
| E2E回归 | 10 | 10 | 0 | Docker Desktop Kubernetes v1.36.1 | `npm run test:e2e` |

> 本机基线合计72项；Kubernetes回归为同一套E2E的第二环境执行。按实际执行次数计算为82/82通过，不能将其误解为82个不同测试用例。

## 覆盖说明

- 10个用例各有1条API主成功流程和1条完整E2E链路。
- UC-03、UC-04、UC-05、UC-07、UC-10各增加1条异常/备选API流程。
- 9个单元测试套件覆盖公告、资料、选课/课表、作业、实验、练习、讨论通知和成绩规则。
- 4条DAO测试覆盖事务与复合键、公告已读upsert、资料计数与收藏、讨论关联与级联删除。
- 追溯表10行的13个必填字段全部有值，完整度均为100%。

## 缺陷与处置

| 问题 | 影响 | 处置 | 回归 |
|---|---|---|---|
| Windows Python命令别名导致练习代码题 `spawn UNKNOWN` | UC-07提交接口500 | JavaScript改用当前Node绝对路径；收集输出；运行时不可用时安全返回失败结果 | E2E-07-01通过 |
| 初版E2E断言使用 `ENROLL/DROP` | 管理员操作日志断言失败 | 改为真实枚举 `ADMIN_ENROLL/ADMIN_DROP` | E2E-01-01通过 |
| 讨论详情评论位于 `post.comments` | 详情断言取错层级 | 按真实响应结构修正 | E2E-08-01通过 |

## 构建与交付门禁

- `npm run build`：后端、前端、Judge Worker全部成功。
- 前端产生单个chunk大于500kB的性能提示，但不影响构建与本次功能验收。
- CI质量任务失败会阻断镜像和部署；故障注入验证已确认下游未执行。
- Kubernetes内API、Web、Judge Worker、PostgreSQL、Redis共5/5工作负载就绪，两个PVC均已绑定。
- K8s E2E机器可读报告与Web页面截图均已归档；最终任务无待办项。
