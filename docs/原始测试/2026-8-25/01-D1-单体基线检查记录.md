# D1 单体基线检查记录

- 日期：2026-08-25
- 负责人：范文歆
- 对应任务：D1-BASE-01、D1-BASE-02、D1-BASE-03、D1-BASE-05

## 1. Git 基线

| 项目 | 结果 |
| --- | --- |
| 当前分支 | `master`，跟踪 `origin/master` |
| HEAD | `756712d3a2cd9fc441eaaeb54b16492348453803` |
| 提交说明 | `完善测试用例seed.ts、课程页面重构、测试文档` |
| 提交时间 | 2026-06-12 10:25:58 +08:00 |
| 远端仓库 | `git@github.com:Vesperx24/platForm.git` |
| 已有标签 | 无 |
| `monolith-start` | 暂不创建，等待全部确认用例验证和基线文件确认 |

检查时存在以下未跟踪交付材料，未擅自删除：

- `USER_MANUAL.md`
- `VIBE_CODING_REPORT.md`
- `docs/用户手册.pdf`
- `分工.md`
- `学生端演示.mp4`
- `演示视频.mp4`
- `小学期/` 下的管理材料

在创建 `monolith-start` 前，需要由组长决定上述文件哪些应纳入基线提交、哪些应放入交付目录、哪些应加入忽略规则。

## 2. 构建验证

| 组件 | 命令 | 结果 | 备注 |
| --- | --- | --- | --- |
| 后端 API | `npm run build --prefix backend` | 通过 | TypeScript 正式构建通过。旧 `dist` 文件由管理员账户拥有，受限环境覆盖时会出现 EPERM；在正常权限下覆盖成功，不是编译错误 |
| 前端 | `npm run build --prefix frontend` | 通过 | Vite 构建 813 个模块；主 JS 约 1.18 MB，存在大于 500 KB 的非阻塞警告 |
| 评测 Worker | `npm run build --prefix judge-worker` | 通过 | TypeScript 构建通过 |
| 后端单元测试 | `npm exec -- tsx --test src/lib/lab-set-status.test.ts` | 1/1 通过 | 当前仅发现 1 个单元测试文件，后续必须补齐 |
| Compose 配置 | `docker compose config --quiet` | 通过 | Compose YAML 语法及变量解析正常 |

## 3. 本地运行与健康检查

由于 Docker Hub 返回 `x509: certificate signed by unknown authority`，本次无法重新拉取 Node 基础镜像并构建完整应用镜像。已使用仓库推荐的本地开发方式验证原系统运行链路。

| 组件 | 验证方式 | 结果 |
| --- | --- | --- |
| PostgreSQL | `docker compose exec -T db pg_isready -U platform -d teaching_platform` | 接受连接 |
| Redis | `docker compose exec -T redis redis-cli ping` | `PONG` |
| API 存活 | `GET http://127.0.0.1:3000/health/live` | HTTP 200，`ok=true` |
| API 就绪 | `GET http://127.0.0.1:3000/health/ready` | HTTP 200，DB 和 Redis 就绪 |
| API 兼容健康接口 | `GET http://127.0.0.1:3000/health` | HTTP 200 |
| 前端 | `GET http://localhost:5173/` | HTTP 200 |
| 评测 Worker | 本地进程日志 | 已监听 `judge-submissions` 队列 |
| 演示账号 | `student@demo.local` 登录 | 成功，角色为 `STUDENT` |

## 4. 当前阻塞与风险

| 编号 | 问题 | 类型 | 影响 | 建议负责人 |
| --- | --- | --- | --- | --- |
| BLK-D1-01 | Docker Hub TLS 证书链无法验证 | 环境阻塞 | 无法重新构建完整 Compose 应用镜像 | 范文歆 |
| RISK-D1-01 | 当前没有 `monolith-start` 标签 | 基线风险 | 无法固定改造前版本 | 范文歆 |
| RISK-D1-02 | 存在未跟踪交付文件 | 版本风险 | 基线内容不明确，可能漏交或误纳入 | 范文歆 |
| RISK-D1-03 | 单元测试文件仅 1 个 | 质量风险 | 不满足课程测试要求 | 各模块负责人 |
| RISK-D1-04 | 前端主包大于 500 KB | 性能风险 | 首次加载可能较慢，不阻塞 D1 | 胡思琪 |

## 5. `monolith-start` 标签创建条件

以下条件全部满足后才能执行标签任务：

- [ ] 教师或助教已确认最终业务用例清单。
- [ ] UC01—UC10 或确认后的全部用例均已运行并留下证据。
- [ ] 阻塞运行的缺陷均已修复并回归。
- [ ] 基线提交包含的文档和交付文件已经确认。
- [ ] 工作区没有来源不明的修改。
- [ ] 组长记录最终基线提交哈希。
- [ ] 创建附注标签 `monolith-start` 并推送到远端。

在上述条件完成前，D1-BASE-05 的正确状态是“阻塞/待依赖”，不能提前关闭。

