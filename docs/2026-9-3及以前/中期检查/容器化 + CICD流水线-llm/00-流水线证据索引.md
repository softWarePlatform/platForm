# platform 流水线证据索引

归档日期：2026-08-31。本次将工作区已有的 29 份必要材料复制到本目录的 `evidence/` 中，保留原文件及现有说明书，不重新执行测试、不触发远程流水线、不修改部署环境。

## 查阅入口

| 检查内容 | 证据 | 能证明的范围 |
| --- | --- | --- |
| 自动触发、构建、测试、镜像发布、部署与失败依赖 | [ci-cd.yml](./evidence/配置快照/ci-cd.yml) | 当前工作区配置；`quality → images → deploy`，测试结果使用 `if: always()` 上传 |
| 容器构建与编排 | [Compose 配置](./evidence/配置快照/docker-compose.yml)、[K8s 镜像构建配置](./evidence/配置快照/docker-compose.k8s-build.yml)、[API Dockerfile](./evidence/配置快照/backend/Dockerfile)、[Web Dockerfile](./evidence/配置快照/nginx/Dockerfile)、[Worker Dockerfile](./evidence/配置快照/judge-worker/Dockerfile) | 构建定义与编排配置；API Dockerfile 同时包含 migrate 阶段 |
| Kubernetes 部署、探针与数据库迁移 | [应用清单入口](./evidence/配置快照/k8s/monolith/kustomization.yaml)、[迁移 Job](./evidence/配置快照/k8s/monolith/migrate-job.yaml)、[本地覆盖配置](./evidence/配置快照/k8s/local/kustomization.yaml) | 已复制 monolith 全部 9 个 YAML 和 local 配置；清单本身不代表部署已成功 |
| Compose 集成冒烟 | [ci-integration.json](./evidence/运行结果/ci-integration.json) | 2026-08-26 历史记录，12 通过、0 失败 |
| 构建及完整测试记录 | [D6 测试完成记录](./evidence/运行结果/00-D6-测试完成记录.md)、[API 结果](./evidence/运行结果/api-use-cases.json)、[本地 E2E 结果](./evidence/运行结果/e2e-local.json) | 2026-08-29 报告记载构建通过、74 项测试通过；JSON 分别记录 API 15/15、本地 E2E 10/10，未补造单元/DAO 原始日志 |
| 本地失败阻断验证 | [失败阻断 JSON](./evidence/运行结果/ci-failure-blocking.json)、[验证脚本](./evidence/配置快照/scripts/verify-ci-failure-block.mjs)、[D4 验证记录](./evidence/运行结果/02-D4-CI-CD测试阶段验证记录.md) | 2026-08-28 本地模拟阶段退出码 17、下游未执行；不是 GitHub Actions 失败运行证据 |
| Kubernetes 实机回归与页面 | [D5 K8s 报告](./evidence/运行结果/03-D5-K8s-E2E回归报告.md)、[K8s E2E JSON](./evidence/运行结果/e2e-k8s.json)、[Web 页面截图](./evidence/运行结果/K8s-Web页面.png)、[恢复后 E2E JSON](./evidence/运行结果/e2e-kubernetes-restored.json) | 2026-08-28 与 2026-08-29 历史集群回归记录均为 10/10；截图为页面证据，不是流水线截图 |
| 测试入口与冒烟实现 | [package.json](./evidence/配置快照/package.json)、[集成冒烟脚本](./evidence/配置快照/scripts/ci-integration.mjs) | 当前工作区命令入口与脚本快照 |

## 时间与版本说明

- 配置快照取自本次归档时的工作区，仓库 HEAD 为 `bbca53a810bc46a151144d4a623d453b5c0ddec6`。该 SHA 仅记录归档时的仓库位置，不表示历史测试结果由此提交产生。
- 原有 `03-CI-CD验收记录.md` 等文档保留了当时的环境限制。后续 K8s 实机结果见本次补充的 D5 报告及 JSON；不能把早期“没有集群”的记载当成后续验收结论。
- 历史说明与当前配置存在差异时，以配置快照判断当前行为。例如当前部署使用带 `k8s-local` 标签的 Windows 自托管 runner；自动触发缺少生产配置时跳过部署，手动明确要求部署而配置缺失时失败。
- D4 记录中的 `npm run test:ci:failure-block` 是历史命令用法。当前 `package.json` 将该命令映射为 `npm test`；独立的本地失败注入脚本为 `scripts/verify-ci-failure-block.mjs`。本次仅复制脚本和旧结果，没有重新运行。
- 本次归档不包含 GitHub Actions 成功/失败运行页面、运行编号与日志包，也不包含 GHCR 四镜像同 SHA 推送记录或远程部署的 migration/rollout 日志，因此这些远程验收项不能由本包认定完成。

## 完整性与使用范围

[证据清单.json](./evidence/证据清单.json) 列出了全部 29 个源文件路径、归档路径、字节数和 SHA-256。复制后已逐份核验源文件与副本的 SHA-256 一致。

配置与脚本用于检查、追溯，不是可独立运行的完整源码包；实际构建与测试仍应在项目根目录进行。本包不复制 `.env`、kubeconfig 或实际生产密钥。已有 `docs/中期检查.zip` 未更新，若提交压缩包需重新打包。
