# DevOps 验收记录（2026-09-03）

环境：Windows、Docker Desktop 4.67.0、Docker Engine 29.3.1、Kubernetes 1.34.3、Node.js 24。

| 项目 | 结果 | 证据/说明 |
| --- | --- | --- |
| Compose 语法 | 通过 | `docker compose config --quiet` |
| Kustomize 渲染 | 通过 | `k8s/monolith`、`k8s/local` 均成功 |
| 全量应用构建 | 通过 | 旧 API、前端、Gateway、Course、Homework、Lab、Worker |
| 独立服务测试 | 通过 | Gateway 4、Course 5、Homework 25、Lab 43、Worker runner |
| Docker 镜像 | 通过 | Gateway、三个业务服务、Worker、Web 及四组 migration 镜像构建成功 |
| Lab 正式 migration | 通过 | 初始 migration 已提交；全新临时库验证通过；旧 Compose/K8s 库在无 Schema 漂移后建立基线，正式 Lab migration Job 再次完成 |
| Compose 运行 | 通过 | 所有服务健康；四组 migration 退出码 0；Gateway `/api/courses` 返回 200 |
| Kubernetes migration | 通过 | database-bootstrap、legacy、Course、Homework、Lab Job 完成 |
| Kubernetes rollout | 通过 | 7 个应用 Deployment 全部成功 |
| 本地不可变部署 | 通过 | 以同一 `local-1788422207` 唯一标签完成 7 个应用滚动发布，未复用旧 `:dev` 镜像 |
| 集群内健康 | 通过 | Gateway、Course、Homework、Lab、兼容 API、Web 全部 HTTP 200 |
| Metrics Server | 通过 | `kubectl top nodes/pods` 返回指标 |
| HPA | 通过 | Gateway HPA 的 CPU TARGETS 有数值；实际扩为 2 个 Ready Pod，稳定窗口后缩回 1 个 |
| CI/CD 配置 | 已实现、待远程触发 | 本地完成语法/依赖链审计；质量门会迁移/启动全部服务并做 Gateway 冒烟测试；GHCR 推送与 GitHub Actions 运行需提交后由远程平台执行 |

本次运行证据：

- `test-results/deployment-local/health-summary.json`
- `test-results/deployment-local/cluster-resources.log`
- `test-results/deployment-local/service-health-checks.log`
- `test-results/hpa/hpa-during-load.log`
- `test-results/hpa/hpa-after.yaml`

说明：HPA 的“配置、指标获取和实际多副本”均已验证。短压测开始时 Gateway 已因启用 Metrics Server 后的初始负载扩为 2 个副本，因此本轮证据能证明弹性控制器工作和多副本就绪，但不能把该时间段描述为从 1 到 2 的完整时间线。需要答辩截图时，待 300 秒稳定窗口缩至 1 后，再运行 180 秒实验记录完整前后变化。
