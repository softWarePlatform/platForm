# C 工作简报：8 月 31 日 — 9 月 1 日

- 身份：Homework + Integration Owner
- 范围：`homework-grade-service` + `api-gateway`

## 已完成

- 抽出独立 `homework-grade-service`（`:3002`），库 `homework_grade_service`，作业域表只在本服务。
- 健康检查、核心 API 冒烟：创建/发布作业、提交、批改、发布成绩、成绩配置、成绩册。
- 课程权限/名单/通知改为 HTTP 调 Course；实验成绩改为 HTTP 调 Lab；Lab 不可用时 `labStatus=UNAVAILABLE`，不当 0 分。
- 单体 `backend` **仍保留**作业路由（双跑）。浏览器流量应走网关，不要同时打 `:3000` 的作业接口。
- 独立 `api-gateway`（`:3081`）：`/api` 按服务转发、Request-ID、基础限流、下游 502。Vite `/api` 已指向网关。
- 跨服务字段对齐见同目录 `01-C-跨服务内部API对齐.md`（请 A 9/2 冻结前确认，请 B 确认成绩/错题契约）。
- 已补齐对内成绩接口：单人已发布成绩、批量 gradebook、`final-gradebook`（Lab 不可用时 `totalScore=null` + `provisionalTotal`）。名单兼容 `items`/`students`；通知同时发 `Idempotency-Key` 头和 body。

## 证据（本机）

```text
course-service     GET :3001/health/live|ready  200
homework-grade     GET :3002/health/live|ready  200
homework-grade     npm run test:api             passed
api-gateway        GET :3081/health/live        200  service=api-gateway
api-gateway        npm test                     4 passed
api-gateway        npm run test:api             passed
```

网关冒烟覆盖：登录（course）、教师作业列表 200 / 学生 403、选课目录、课程列表、Lab 未启动时 `/api/labs` → 502、CORS 预检。

## 明日（D8 / 9 月 2 日）

- A 冻结后：权限改为 `/internal/.../access`（公开 catalog 可撤）；确认 token 字符串三服务一致。
- 按 B 答复切 `lab-gradebook` 或 `lab-grades:batch`；错题接口到了再去掉 501。
- A Dashboard 可联调 C 的 `homework-summary` / `final-gradebook`。
- 配合 B 做 Gateway → Homework → Lab 故障演示。
