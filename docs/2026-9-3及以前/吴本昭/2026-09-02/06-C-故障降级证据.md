# 故障降级证据（W3-2，交 B/E 对齐）

- 日期：2026-09-02
- C 负责：网关 502 形状、作业不崩、总评降级
- B 负责：停 Lab / 停 Judge 进程本身（今日 Lab 未起 ≡ 已停）
- 步骤文档：`02-C-故障演示-Gateway-Homework-Lab.md`

## 1. 网关下游 Lab 502

经 Vite `/api`（与浏览器同源）打到网关：

```json
{
  "status": 502,
  "requestId": "w32-lab-down",
  "body": {
    "code": "BAD_GATEWAY",
    "message": "lab 服务暂时不可用",
    "requestId": "w32-lab-down"
  }
}
```

`X-Request-ID` 与 body.`requestId` 均为 `w32-lab-down`。读错题 `GET /api/wrong-book/mine` 同样 502（读路径归 Lab）。

原始：`raw/w3-degrade-evidence.json`、`raw/vite-proxy-smoke.log`、`raw/gateway-smoke.log`

## 2. 作业总评降级（不当 0）

`GET /api/courses/{courseId}/gradebook` **200**：

| 字段 | 值 |
| --- | --- |
| labStatus | `UNAVAILABLE` |
| totalScore | `null` |
| labAverage | `null` |
| homeworkAverage | 约 89.71（已批改作业） |
| provisionalTotal | 约 53.83（作业均分 × 作业权重 0.5） |

实验分没有被写成 0。`combineTotal` 单测覆盖：UNAVAILABLE 时 `totalScore=null`；OK 但该生实验分为 null 时总分仍为空。

## 3. Lab 挂了作业仍能提交/批改

W2-2 经 Vite 代理的 UC05：创建 → 发布 → 提交 → 批改 → 发布成绩，在 Lab 未起时全部成功。作业 id：`1f03a1ef-eb48-453c-8bc8-317556e2865a`。

## 4. 还缺什么（不要对 E 说已经做完）

完整「Lab 先 200 再掐断」要等 B 把 `:3003` 拉起来（规划 W1-8）。C 不替 B 停 Judge。
