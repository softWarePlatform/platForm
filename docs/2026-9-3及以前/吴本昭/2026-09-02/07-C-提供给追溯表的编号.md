# 提供给追溯表的编号（任务 C · UC05 / UC09）

请抄入 `docs/胡思琪/2026-08-25/02-需求-设计-代码-测试追溯表.md`。只交本模块，不替 A/B 补行。格式与前五天 `docs/吴本昭/前五天/最终提交/03-提供给追溯表的编号.md` 同一套：`REQxx` → `UCxx` → `DES-*` → 路由文件 → `TC-*`。

日期：2026-09-02。责任人：吴本昭。

## 建议一行（设计项列可直接粘）

`REQ05` → `UC05` → `DES-SD-UC05` + `DES-ST-UC05` → `homework-grade-service/src/routes/homework.ts` → `TC-HW` / `TC-GW-UC05`

`REQ09` → `UC09` → `DES-SD-UC09` + `DES-AD-UC09` → `homework-grade-service/src/routes/grades.ts` → `TC-GRADE` / `TC-GW-UC09`

## 本模块代码（替换表里仍指向 `backend/src/routes/homework.ts` 的路径）

| 需求 ID | 需求描述 / 验收准则 | 设计项 | 代码实现（主要位置） | 测试与证据 | 状态 |
| --- | --- | --- | --- | --- | --- |
| UC05-R01 | 教师可创建并发布作业；学生可查看并提交。浏览器只打网关 `/api`。 | DES-COMP-UC05、DES-SD-UC05、DES-ST-UC05；Gateway 分流 homework | `frontend/src/components/homework/HomeworkPublishForm.tsx`；`HomeworkStudentSubmit.tsx`；`homework-grade-service/src/routes/homework.ts`；`api-gateway/src/routing.ts` | `TC-HW-001~004`；`homework-grade-service/tests/api-smoke.mjs`；`api-gateway/tests/api-smoke.mjs`（`:3081` 与 Vite `:5173/api`）；`docs/吴本昭/2026-09-02/raw/vite-proxy-smoke.log` | 已验证（API；前端代理同源） |
| UC05-R02 | 教师可批改并发布成绩；学生可申请重做。 | DES-ST-UC05；批改/发布/重做 | `frontend/src/components/homework/HomeworkTeacherGradingPanel.tsx`；`homework-grade-service/src/routes/homework.ts`；`homework-student.ts` | `TC-HW-003/005/006`；网关冒烟含 grade + release-grades | 已验证（API） |
| UC09-R01 | 教师可设置实验/作业权重，两项之和为 1。 | DES-AD-UC09；GradingConfig | `frontend/src/components/grades/GradebookPanel.tsx`；`homework-grade-service/src/routes/grades.ts` | `TC-GRADE-001`；网关冒烟断言 `weights.lab + weights.homework === 1` | 已验证 |
| UC09-R02 | 成绩册按同一规则显示作业、实验、总评；Lab 不可用时总分 null 而不是 0。 | DES-SD-UC09；Lab 降级 | `homework-grade-service/src/lib/lab-client.ts`（`combineTotal` / `fetchLabGradebook`）；`src/lib/gradebook.ts`；`src/routes/grades.ts` | `TC-GRADE-002`；`tests/combine-total.test.ts`；`raw/w3-degrade-evidence.json` | 已验证（降级口径）；真 Lab 分阻塞 |
| UC09-R03 | 网关在 Lab 不可用时对实验路径返回 502，且带 requestId；作业路径仍可用。 | Gateway 502、Request-ID | `api-gateway/src/index.ts`；`api-gateway/src/routing.ts` | `TC-GW-502`；`api-gateway/tests/*.test.ts`；`raw/w3-degrade-evidence.json` | 已验证 |

## 测试编号（C 侧）

| 编号 | 含义 | 位置 |
| --- | --- | --- |
| TC-HW-API | 作业服务直连冒烟 | `homework-grade-service/tests/api-smoke.mjs` |
| TC-GW-UC05 | 经网关 / 经 Vite 代理的 UC05 | `api-gateway/tests/api-smoke.mjs` |
| TC-GW-ROUTE | 作业路径优先于 `/api/courses` 通配 | `api-gateway/tests/routing.test.ts` |
| TC-GW-502 | Lab 挂了 502 `BAD_GATEWAY` | 同上冒烟 + `raw/w3-degrade-evidence.json` |
| TC-GRADE-DEGRADE | UNAVAILABLE → totalScore null | `tests/combine-total.test.ts`、`lab-client.test.ts` |
| TC-ACCESS-FC | Course access fail-closed | `tests/course-access.test.ts` |

## 给 E 的注意

- 不要把 `backend/src/routes/homework.ts` 再当作业运行时路径；单体默认不再注册作业路由（`MONOLITH_HOMEWORK_ROUTES`）。
- UC06–UC08、UC01–UC04、UC10 不在本表。
- UC09 真实验分一行保持「阻塞」，直到 B `:3003` 给出 `labStatus=OK`。
