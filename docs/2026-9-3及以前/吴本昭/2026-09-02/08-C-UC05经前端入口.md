# C 侧 UC05 经前端入口（W2-2）

- 日期：2026-09-02
- 路径：`浏览器 / 冒烟 → Vite :5173 /api → Gateway :3081 → Course :3001 / Homework :3002`

## 做了什么

前端 `vite.config.ts` 把 `/api` 代理到 `http://127.0.0.1:3081`。本机 Vite 只绑 `localhost`（不是 `127.0.0.1:5173`）。

在前端进程已起、Lab 未起时，用

```text
GATEWAY_URL=http://localhost:5173  npm run test:api
```

（在 `api-gateway` 目录）跑完与浏览器相同的 `/api` 入口：

登录（A）→ 创建作业 → 发布 → 学生提交 → 教师批改 → 发布成绩 → 成绩册 200 且 `labStatus=UNAVAILABLE`。

作业 id：`1f03a1ef-eb48-453c-8bc8-317556e2865a`  
日志：`raw/vite-proxy-smoke.log`

登录页 `http://localhost:5173/login` 已打开。自动化不能向页面填演示口令，故未再点 UI；代理冒烟覆盖同一条前端 `/api` 链路。答辩若要「点一遍」，用 `teacher@course.local` / `student@course.local` / `Course123456`，课程里点「作业管理」。

## 最小手工步骤（可选补录）

1. 教师登录 → 打开课程 → 作业管理 → 标题 + 勾选「保存后立即发布」→ 保存作业
2. 退出，学生登录 → 同一课程作业 → 正式提交
3. 教师进入该作业 → 分数 → 保存 → 发布已批改成绩
4. 学生再进详情，应能看到已发布分数
5. 教师成绩统计：总评在 Lab 未起时应为空，不要出现实验 0 分
