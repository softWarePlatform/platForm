# A：仍需处理事项清单

- 更新日期：2026-09-02
- 本清单区分“由 A 继续配合”和“必须由其他负责人修复”；A 不越界修改其他服务。

| 优先级 | 事项 | 负责人 | 完成标准 | A 的下一步 |
| --- | --- | --- | --- | --- |
| P0 | Lab 成绩接口冻结 | B | 提供 `GET /internal/courses/:courseId/lab-gradebook`，或书面确认并实现 C/A 可用的等价批量聚合契约 | 收到冻结契约后，A 为 Dashboard 增加对应 HTTP 适配与契约测试 |
| P0 | Lab 统一鉴权与端口 | B | 支持 `x-internal-service-token`、共享令牌，服务监听 `3003` | A 保持现有安全配置；不发送 B 私有头或令牌 |
| P0 | Lab 统一错误体和 Request-ID | B | 内部失败返回 `{ code,message,requestId }` 并透传 `x-request-id` | A/C 可按错误码区分 401、超时与上游不可用 |
| P1 | A ↔ C 真实 Dashboard 联调 | A + C | `:3001` 调 `:3002` 后，Dashboard 的 homework 依赖为 `OK` 并显示 C 摘要 | C 启动服务并统一令牌后，A 执行联调和证据补充 |
| P1 | C 切换课程权限客户端 | C | 创建/提交/批改等写操作改用 A `/internal/**`，依赖失败 fail-closed | A 已可支持；待 C 提交后核对字段和错误处理 |
| P1 | Gateway 安装依赖并回归 | C / 环境负责人 | `api-gateway npm ci && npm run build` 通过；`:3081` 可路由 A 公开 API | A 配合测试 `/api/enrollment/**`、`/api/dashboard/**` |
| P2 | K8s 实际集群验证 | D | A 的 Deployment/Service/Secret/ConfigMap 在集群通过探针 | A 按 D 的规范调整本服务配置，不自行接管集群 |
| P2 | 全服务 E2E | E | 覆盖正常、鉴权、超时、502/503 与部分结果降级 | A 提供课程服务 API、迁移和契约测试入口 |

## A 已完成，无需重复开发

- 课程、用户、权限、班级、名单、通知内部 API；
- 通知批量幂等和请求追踪；
- 作业 Dashboard 摘要调用与 `UNAVAILABLE` 降级；
- 课程服务数据库迁移、种子、构建和 5 条契约测试。
