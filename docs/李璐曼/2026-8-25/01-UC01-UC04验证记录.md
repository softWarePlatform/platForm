# UC01—UC04 验证记录

## 1. 验证信息

| 项目 | 内容 |
| --- | --- |
| 日期 | 2026-08-25 |
| 执行时间 | 16:05:46—16:05:50（Asia/Shanghai） |
| 验证方式 | API 驱动的系统测试，结合 PostgreSQL 实际枚举与表记录检查 |
| API 地址 | `http://127.0.0.1:3000` |
| 测试账号 | `teacher@demo.local`、`student20@demo.local`、`student@demo.local`、`admin@demo.local` |
| 数据策略 | 使用唯一课程代码创建临时数据；结束后自动清理课程、选课、候补、公告、资料文件、收藏和通知 |
| 原始证据 | [`evidence/uc01-uc04-api-results.json`](./evidence/uc01-uc04-api-results.json) |

## 2. 前置条件

| 检查项 | 预期 | 实际 | 结果 |
| --- | --- | --- | --- |
| API 与数据库就绪 | `/health/ready` 返回 HTTP 200、`ok=true` | HTTP 200、`ok=true` | 通过 |
| 教师登录 | 返回 JWT | HTTP 200，已返回 token | 通过 |
| 测试学生登录 | 返回 JWT | HTTP 200，已返回 token | 通过 |
| 候补 fixture 学生登录 | 返回 JWT | HTTP 200，已返回 token | 通过 |
| 管理员登录 | 返回 JWT | HTTP 200，已返回 token | 通过 |
| 选课窗口 | 当前窗口开放 | `FORMAL` 阶段处于开放时间内，无需修改 | 通过 |

## 3. 汇总结果

| 用例 | 主流程 | 异常流程 | 合计 | 用例结论 |
| --- | ---: | ---: | ---: | --- |
| UC01 选课、退课、候补及课表同步 | 3/3 | 3/3 | 6/6 | 通过 |
| UC02 创建、配置并发布课程 | 2/3 | 3/3 | 5/6 | **失败**：创建接口异常 |
| UC03 发布、阅读公告及已读记录 | 2/2 | 3/3 | 5/5 | 通过 |
| UC04 上传、浏览、收藏和下载资料 | 3/3 | 3/3 | 6/6 | 通过 |
| **总计** | **10/11** | **12/12** | **22/23** | **未全部通过** |

说明：UC02 创建请求虽然返回 HTTP 500，但课程记录已经写入数据库。因此，本次验证利用该条临时记录继续检查了配置、发布以及 UC01、UC03、UC04；这不改变 UC02 创建步骤失败的结论。

## 4. UC01 验证记录

### 4.1 主流程

| 编号 | 场景与操作 | 预期 | 实际 | 结果 |
| --- | --- | --- | --- | --- |
| UC01-M-01 | 学生选择临时课程，再读取 `/dashboard/me` | 选课 HTTP 200；个人课表包含课程及课表时段 | 选课 HTTP 200；dashboard 包含课程，含星期日第 13—14 节课表 | 通过 |
| UC01-M-02 | 学生退选课程，再读取 `/dashboard/me` | 退课 HTTP 200；个人课表不再包含课程 | 退课 HTTP 200；dashboard 已移除课程 | 通过 |
| UC01-M-03 | 对已满课程 CS901 先选课，再加入候补 | 直接选课 HTTP 409；候补 HTTP 200 | 依次返回 HTTP 409 和 HTTP 200 | 通过 |

### 4.2 异常流程

| 编号 | 场景与操作 | 预期 | 实际 | 结果 |
| --- | --- | --- | --- | --- |
| UC01-E-01 | 重复选择同一门课程 | HTTP 409 | HTTP 409，`已选过该课程` | 通过 |
| UC01-E-02 | 未选课程时再次退课 | HTTP 404 | HTTP 404，`未选该课程` | 通过 |
| UC01-E-03 | 重复加入同一课程候补 | HTTP 409 | HTTP 409，`已在候补列表中` | 通过 |

### 4.3 运行结论

选课、退课、已满候补以及个人课表同步均符合预期。UC01 未发现新缺陷。

## 5. UC02 验证记录

### 5.1 主流程

| 编号 | 场景与操作 | 预期 | 实际 | 结果 |
| --- | --- | --- | --- | --- |
| UC02-M-01 | 教师创建一门 `published=false` 的课程 | HTTP 200，返回课程 | **HTTP 500**；随后查询 `/courses/mine` 却发现课程已经写入数据库 | **失败** |
| UC02-M-02 | 配置容量、学分和课表 | HTTP 200；`capacity=50`、`credits=3`、教室为 `D1-T02` | HTTP 200，字段均正确 | 通过 |
| UC02-M-03 | 将课程发布，再从公开课程列表搜索 | 发布 HTTP 200；公开列表可见 | 发布 HTTP 200；公开列表包含课程 | 通过 |

### 5.2 异常流程

| 编号 | 场景与操作 | 预期 | 实际 | 结果 |
| --- | --- | --- | --- | --- |
| UC02-E-01 | 使用重复课程代码创建课程 | HTTP 409 | HTTP 409，`课程代码已存在，请更换` | 通过 |
| UC02-E-02 | 学生越权创建课程 | HTTP 403 | HTTP 403 | 通过 |
| UC02-E-03 | 教师提交空课程标题 | HTTP 400 | HTTP 400 | 通过 |

### 5.3 运行结论

课程配置、发布、公开可见性、重复代码校验、角色权限和输入校验均通过；但创建接口存在关键错误，UC02 当前不能验收通过。详细缺陷见 [`03-缺陷记录.md`](./03-缺陷记录.md)。

## 6. UC03 验证记录

### 6.1 主流程

| 编号 | 场景与操作 | 预期 | 实际 | 结果 |
| --- | --- | --- | --- | --- |
| UC03-M-01 | 教师发布公告，学生读取公告列表 | 发布 HTTP 200；学生列表可见且 `read=false` | HTTP 200；列表包含公告且 `read=false` | 通过 |
| UC03-M-02 | 学生打开公告详情，再读取列表 | 详情 HTTP 200；形成已读记录，列表 `read=true` | 详情 HTTP 200；随后列表 `read=true` | 通过 |

### 6.2 异常流程

| 编号 | 场景与操作 | 预期 | 实际 | 结果 |
| --- | --- | --- | --- | --- |
| UC03-E-01 | 学生越权发布公告 | HTTP 403 | HTTP 403 | 通过 |
| UC03-E-02 | 教师发布空正文公告 | HTTP 400 | HTTP 400 | 通过 |
| UC03-E-03 | 未选课学生查看课程公告 | HTTP 403 | HTTP 403 | 通过 |

### 6.3 运行结论

公告发布、列表展示、详情阅读、已读记录以及权限与输入校验均符合预期。UC03 未发现新缺陷。

## 7. UC04 验证记录

### 7.1 主流程

| 编号 | 场景与操作 | 预期 | 实际 | 结果 |
| --- | --- | --- | --- | --- |
| UC04-M-01 | 教师上传文本资料，学生读取资料列表 | 上传 HTTP 200；列表包含资料 | HTTP 200；学生列表包含资料 | 通过 |
| UC04-M-02 | 学生收藏资料，再读取收藏列表 | 收藏 HTTP 200；收藏列表包含资料 | HTTP 200；收藏列表包含资料 | 通过 |
| UC04-M-03 | 学生下载资料并比对内容 | HTTP 200；内容与上传内容一致 | HTTP 200；33 bytes，内容一致 | 通过 |

### 7.2 异常流程

| 编号 | 场景与操作 | 预期 | 实际 | 结果 |
| --- | --- | --- | --- | --- |
| UC04-E-01 | 学生越权上传资料 | HTTP 403 | HTTP 403 | 通过 |
| UC04-E-02 | 未选课学生浏览资料 | HTTP 403 | HTTP 403 | 通过 |
| UC04-E-03 | 下载不存在的资料 | HTTP 404 | HTTP 404 | 通过 |

### 7.3 运行结论

资料上传、浏览、收藏、下载、访问控制及不存在资源处理均符合预期。UC04 未发现新缺陷。

## 8. 清理记录

| 临时数据 | 结果 |
| --- | --- |
| 课程 `75da821e-7d85-4c64-baec-7de3b43594d2` | 已从数据库删除 |
| 公告 `8c9bebc6-ba5c-49f7-b926-1e80a6d96bb8` | 已删除 |
| 资料 `bceb06d9-dbad-435a-bf75-ed47ee0168cc` | 已删除，上传文件同步删除 |
| 收藏、选课、候补、已读记录和选课日志 | 已通过接口或课程级联删除 |
| 本次产生的站内通知 | 已清理 |
| 选课时段 | 本次未修改 |

清理后的课程域表计数保存在 [`evidence/course-domain-table-counts.json`](./evidence/course-domain-table-counts.json) 中。

## 9. 验收建议

- UC01、UC03、UC04 可判定为 D1 当前环境验证通过。
- UC02 应保持“未通过/待修复”，修复 `BUG-D1-LI-001` 后重新执行本目录脚本。
- UC02 回归必须同时确认：创建请求返回 HTTP 200、仅生成一条课程记录、生成 `COURSE_CREATE` 日志、重复提交不会制造脏数据。
