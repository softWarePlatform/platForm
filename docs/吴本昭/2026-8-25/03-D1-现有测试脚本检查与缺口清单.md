# D1 现有测试脚本检查与缺口清单

- 日期：2026-08-25
- 负责人：吴本昭
- 对应任务：检查现有测试脚本，产出缺口清单（供 D3 补齐）

## 1. 资产盘点

| 路径 | 类型 | 与 UC06—UC08 关系 | 本次执行 |
| --- | --- | --- | --- |
| `backend/src/lib/lab-set-status.test.ts` | 单元测试（实验集时间窗） | UC06 窗口/补交规则 | **1/1 通过** |
| `tests/api/00-health.http` | REST Client，需人工 | 环境 | 未用 REST Client；等价接口已由脚本覆盖 |
| `tests/api/01-auth.http` | 同上 | 登录 | 脚本已登录 |
| `tests/api/03-homework-labs.http` | 同上 | UC06 提交/未开始/打回查询 | 逻辑已由 `d1-uc06-uc08-verify` 与 `retest-lab-submit` 覆盖 |
| `tests/api/04-practice-notify.http` | 同上 | UC07 组卷、UC08 通知列表 | 仅 GET/组卷，无答题提交、无讨论发帖 |
| `scripts/test-fixtures.mjs` | 固定 seed ID | 被多脚本引用 | 与 seed 输出一致 |
| `scripts/retest-lab-submit.mjs` | Node，评测联调 | UC06 | JS **ACCEPTED**；Python **ERROR** |
| `scripts/d1-uc06-uc08-verify.mjs` | 本次新增 D1 验证 | UC06—UC08 | **27/27 通过** |
| `scripts/step5-api-test.mjs` | 综合 API，会改选课期等 | 含实验/练习 | **未跑**（会 `PUT /enrollment/period` 关窗，干扰李璐曼 UC01） |
| `scripts/step6-system-test.mjs` | 系统测试 | 含 LAB/PRACTICE/NOTIFY | 已跑，exit 0；**TC-UX-001 失败**（前端 IPv4 不可达）；若干 LAB/PRACTICE 项标注「步骤5已验证」**并未真正再测评测** |
| `scripts/step8-perf-sec-test.mjs` | 性能/安全抽检 | 非 UC06—08 主路径 | 未作为本任务必跑 |
| `data/judge-demo/` | 手工自测包 | UC06 | 未走浏览器上传；API 已用等价 JS 代码 |
| `docs/范文歆/2026-8-25/04-测试报告.md` | 历史测试报告 | 过时（仍写 React 18 等） | 仅阅读 |

仓库内**没有** Playwright/Cypress 等浏览器 E2E（D3 胡思琪）。根目录**没有**统一 `npm test`。

## 2. 本次实际运行摘要

| 命令 | 结果 |
| --- | --- |
| `npm exec -- tsx --test src/lib/lab-set-status.test.ts`（backend） | pass 1 |
| `node scripts/d1-uc06-uc08-verify.mjs` | 27/27 |
| `node scripts/retest-lab-submit.mjs` | JS AC；Python ERROR |
| `node scripts/step6-system-test.mjs` | 进程成功；`webOk=false`，TC-UX-001 失败 |

`step6` 原始输出：`raw/step6-system-test.txt`（控制台重定向编码为乱码，JSON 字段名仍可读）。

## 3. 测试缺口清单（D3 优先）

编号规则：`GAP-UC0x-NN`。

### UC06 实验与评测

| 编号 | 缺口 | 现状 | D3 建议 |
| --- | --- | --- | --- |
| GAP-UC06-01 | 无 Worker 单元测试 | runner/worker 无断言 | 对 `normalizeOutput`、无用例默认满分、超时分支做单测 |
| GAP-UC06-02 | 无队列故障断言 | 停 Redis/停 Worker 时 API 行为未自动测 | 提交后保持 PENDING；`/health/ready` 503 |
| GAP-UC06-03 | Python 评测在 Windows 不可重复 | `python3` 不存在则 ERROR | runner 回退 `python`；CI 用 JS 或容器 |
| GAP-UC06-04 | 文件提交 `submit-file` 无自动脚本 | 仅有代码 JSON 提交 | 补 multipart 用例 |
| GAP-UC06-05 | 手动批改 `PATCH .../grade` 未测 | UC06 只覆盖 AUTO | 补 MANUAL 实验集 |
| GAP-UC06-06 | 打回次数上限未断言 | 有 `maxReturnCount` 逻辑 | 用 fixture 打到上限期望 400 |
| GAP-UC06-07 | 补交时间窗（makeupDueAt）无独立 API 测试 | 单测覆盖状态机，接口未覆盖 | 过截止、在补交窗内可提交 |
| GAP-UC06-08 | 隐藏用例对学生不可见 | step6 TC-LAB-005 有检查，无独立可重复命令 | 并入 `npm test` |
| GAP-UC06-09 | `.http` 依赖 REST Client 变量，不能 `node` 一键跑 | 人工 | 全部迁到带退出码的脚本 |
| GAP-UC06-10 | step6 对 TC-LAB-003/004 **写死通过** | 不调用 Worker | 改为轮询真实 submission |

### UC07 练习

| 编号 | 缺口 | 现状 | D3 建议 |
| --- | --- | --- | --- |
| GAP-UC07-01 | BY_TAG / WRONG_BOOK / CUSTOM 未自动测 | 只测 SMART | 四种 mode 各一条断言 |
| GAP-UC07-02 | SMART 的 `count` 被服务端忽略 | 请求 5 得到 10 | 文档化或改接口，并加测试 |
| GAP-UC07-03 | 错题组卷依赖错题本，无固定 fixture | 盲答后才有 entries | seed 固定错题 |
| GAP-UC07-04 | 教师题库 CRUD / 导入 / 确认答案 未测 | 仅 GET questions | 补教师端 API |
| GAP-UC07-05 | 反馈处理（教师 PATCH feedback）未测 | step6 只学生提交反馈 | 补闭环 |
| GAP-UC07-06 | 客观题标准答案断言弱 | 盲答 1.5/10，未校验正误规则 | 用已知题 ID 答对/答错 |
| GAP-UC07-07 | 编程练习题本地 runner 未测 | 练习 CODE 类型可能存在 | 与 Worker 边界划清后补测 |
| GAP-UC07-08 | AI 辅导仅降级模板 | 无密钥 | 标记为备选流程；mock LLM |

### UC08 讨论与通知

| 编号 | 缺口 | 现状 | D3 建议 |
| --- | --- | --- | --- |
| GAP-UC08-01 | `.http` 无讨论发帖 | 04 只有通知 GET | 补 POST 讨论 + 通知断言 |
| GAP-UC08-02 | 匿名、置顶、已解决、删除未测 | 本次未覆盖 | 分角色负向用例 |
| GAP-UC08-03 | 讨论附件未测 | multipart | 补上传下载 |
| GAP-UC08-04 | 提及自己不应通知 未断言 | `notifyMentions` 会过滤 fromUserId | 加断言 |
| GAP-UC08-05 | 未选课学生发帖 403 未测 | 权限分支 | 负向用例 |
| GAP-UC08-06 | 通知已读 / 全部已读 未测 | 有接口 | 补 PATCH/POST |

### 工程化缺口（跨用例）

| 编号 | 缺口 |
| --- | --- |
| GAP-ENG-01 | 无根目录 `npm test`，失败不能稳定非零退出（部分脚本总是打印 JSON） |
| GAP-ENG-02 | 无 CI workflow |
| GAP-ENG-03 | 单元测试仅 1 个文件（范文歆 RISK-D1-03，本模块确认仍成立） |
| GAP-ENG-04 | `step5` 会关闭选课窗口，不可与课程组并行 |
| GAP-ENG-05 | 无浏览器 E2E；且当前 Vite 只绑 IPv6，step6 前端检查失败 |
| GAP-ENG-06 | `tests/api/*.http` 登录变量依赖 REST Client `@name`，CLI 无法直接跑 |

## 4. D3 建议顺序

1. 把 `d1-uc06-uc08-verify.mjs` 收成可回归命令（退出码已在失败时为 1）。
2. 拆出不改全局选课期的 lab/practice/discussion 套件。
3. 补 Worker runner 的 `python` 回退与 JS 固定题断言。
4. 删除 step6 中「步骤5已验证」的假通过。
