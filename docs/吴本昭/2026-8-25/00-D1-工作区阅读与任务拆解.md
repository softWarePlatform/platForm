# D1 工作区阅读与任务拆解

- 日期：2026-08-25
- 负责人：吴本昭
- 对应任务：启动 Redis 和 Worker；验证 UC06—UC08；检查现有测试脚本
- 当日完成标准：Worker 能消费评测任务；三个用例均有运行结果和测试缺口清单

## 1. 任务理解

根据《软件工程基础实践项目任务分工》D1（8 月 25 日）安排，实验练习服务与测试负责人需在原系统基线确认阶段完成：

1. 启动 Redis 与评测 Worker。
2. 验证业务用例 UC06、UC07、UC08 的主流程（含必要异常/备选路径）。
3. 检查仓库中已有测试脚本，产出测试缺口清单。
4. 每一步以文档留痕，供看板、站会和后续 D3 自动化补齐使用。

本组其他成员同期工作（不替代本任务）：范文歆负责单体基线与健康检查；李璐曼负责 PostgreSQL 迁移/种子与 UC01—UC04；闾玥负责 UC05、UC09。本任务依赖数据库与 API 可运行，但不重复其课程/作业用例验收。

## 2. 阅读范围

| 材料 | 结论 |
| --- | --- |
| 任务分工（桌面 `小学期任务分工.md` 与仓库 `小学期/`） | UC06 实验评测、UC07 练习、UC08 讨论归吴本昭；验收要有运行结果和缺口清单 |
| `RUN.txt` | 本地开发：Postgres + Redis + `npm run dev`；评测需 `npm run dev:worker` 或 `npm run dev:full` |
| `docker-compose.yml` | Redis `6379`，Postgres 映射 `5433:5432`，独立 `judge-worker` 服务 |
| `backend/src/lib/queue.ts`、`judge-worker/src/worker.ts` | 队列名 `judge-submissions`，BullMQ + Redis，Worker 并发 4 |
| `backend/src/routes/labs.ts`、`lab-sets.ts`、`lab-submit.ts` | 发布实验集、提交代码、轮询结果、教师打回 `PATCH /submissions/:id/return` |
| `backend/src/routes/practice.ts` | 智能组卷、答题、提交批改、错题本、辅导接口 |
| `backend/src/routes/discussions.ts`、`notifications.ts` | 发帖/评论、`mentionUserIds`、站内通知 |
| `tests/api/*.http`、`scripts/step5-api-test.mjs`、`step6-system-test.mjs`、`retest-lab-submit.mjs` | 已有手工 HTTP 集合与 Node 脚本，覆盖不完整 |
| `backend/src/lib/lab-set-status.test.ts` | 当前唯一单元测试文件 |
| `docs/范文歆/2026-8-25/` | D1 文档体例：日期、负责人、任务编号、表格证据 |
| `data/judge-demo/` | 评测自测包：JS Hello / Python A+B 样例 |

## 3. 用例与代码对应（验证入口）

| 编号 | 业务目标 | 主要接口 | 关键代码 |
| --- | --- | --- | --- |
| UC06 | 教师发布实验集，学生提交代码，Worker 自动评测，支持打回和补交 | `POST /courses/:id/lab-sets`、`POST /labs/:id/submit`、`GET /submissions/:id`、`PATCH /submissions/:id/return`、未开始窗口提交应 403 | `lab-sets.ts`、`lab-submit.ts`、`judge-worker/src/worker.ts`、`lab-set-status.ts` |
| UC07 | 学生智能组卷、答题、提交，生成统计和错题并获取辅导 | `POST /courses/:id/practice/sessions`、答题 PATCH、`POST .../submit`、`GET .../stats`、`POST .../hint` | `practice.ts`、`practice-pick.ts`、`practice-grade.ts` |
| UC08 | 学生参与实验讨论，提及并收到通知 | `POST /labs/:labId/discussions`、评论、`GET /notifications` | `discussions.ts` `notifyMentions`、`notifications.ts` |

## 4. 执行步骤（本文件为第 0 步）

| 步骤 | 内容 | 留痕文档 |
| --- | --- | --- |
| 0 | 阅读工作区并拆解任务 | 本文 |
| 1 | 启动 Redis，配置运行环境，启动 Worker 与 API | `01-D1-Redis与Worker启动记录.md` |
| 2 | 验证 UC06—UC08（API 主流程 + 页面抽查） | `02-D1-UC06-UC08验证记录.md` |
| 3 | 检查现有测试脚本并列出缺口 | `03-D1-现有测试脚本检查与缺口清单.md` |
| 4 | 汇总当日完成、计划、阻塞 | `04-D1-当日工作简报.md` |

原始命令输出保存在 `raw/` 目录，报告中引用路径而不只贴截图。

## 5. 验证原则

- 不修改业务源代码（D1 是基线验证，不是功能开发）。
- 评测优先使用 JavaScript 题目，避免 Windows 上 `python3` 命令缺失导致 Worker 误判。
- AI 辅导在未配置大模型密钥时按本地降级验收，并在结果中标注。
- 发现缺陷只记录，不擅自扩大修复范围，除非阻塞无法取得“Worker 能消费任务”这一验收条件。
