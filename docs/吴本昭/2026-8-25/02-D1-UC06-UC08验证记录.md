# D1 UC06—UC08 验证记录

> **D3 起请跑 `npm run test:lab`。** 本文是 2026-08-25 的基线快照，不要当作当前回归入口。最新结果见 [../2026-8-27/README.md](../2026-8-27/README.md)。

- 日期：2026-08-25
- 负责人：吴本昭
- 对应任务：验证 UC06、UC07、UC08
- 证据文件：`raw/uc06-uc08-verify.json`
- 账号：`student@demo.local` / `teacher@demo.local`，密码 `Demo123456`
- 课程：seed 固定 ID `00000000-0000-4000-8000-00000001001c`（程序设计基础 / CS101）

脚本：`node scripts/d1-uc06-uc08-verify.mjs`  
汇总：**27 / 27 通过**（含环境检查 4 项）。未改业务源代码。

前端页面：Vite 仅监听 IPv6，自动化浏览器与 IPv4 HTTP 未能打开 `5173`。下列结论以 **API 主流程** 为准；页面入口见 `frontend/src/App.tsx`（实验 `/student/courses/:courseId/labs`，练习 `.../practice`，讨论 `.../labs/:labId/discussions/:postId`）。

## UC06 教师发布实验集，学生提交，Worker 评测，打回与补交

| 检查项 | 路径 | 结果 | 证据 |
| --- | --- | --- | --- |
| 学生查看实验集 | `GET /courses/:courseId/lab-sets` | 通过，5 个实验集 | count=5 |
| 教师发布实验集 | `POST /courses/:courseId/lab-sets` | 通过，201 | labSetId=`92bce595-ff15-4390-a091-c40a1f8dec2a` |
| 教师创建题目 | `POST /courses/:courseId/labs` | 通过 | labId=`730464a2-d84f-4f9e-8442-773f9eaf0a28`（JS Hello） |
| 配置公开用例 | `POST /labs/:id/testcases` | 通过 | expected=`Hello` |
| 学生提交正确代码 | `POST /labs/:id/submit` | 入队 PENDING | submissionId=`9681e3d6-4e8c-4fef-a143-1a2132cecfe3` |
| Worker 评测 | `GET /submissions/:id` 轮询 | **ACCEPTED，100 分** | 证明队列被消费 |
| 错误代码 | 同上 | **WRONG_ANSWER，0 分** | 主流程反例 |
| 教师打回 | `PATCH /submissions/:id/return` | 通过 | 原因：`D1 验证打回：输出不正确，请按样例修改后补交。` |
| 补交再评 | `POST /labs/:id/submit` | **ACCEPTED** | submissionId=`c5cfe5c5-9c68-4e9c-b107-9b44404ce23e` |
| 未开始窗口 | `POST /labs/{futureLabId}/submit` | **403** `不在可访问时间内` | fixture `00000000-0000-4000-8000-000000090021` |
| 种子打回记录 | `GET /submissions/{returnedSubmissionId}` | 通过 | 含 `returnReason` |

异常/备选：未开始不可提交、错误答案、打回后补交均已覆盖。Python 评测因本机 `python3` 不可用未作为 UC06 通过条件（见启动记录 ENV-D1-02）。

## UC07 智能组卷、答题、提交、统计、错题、辅导

| 检查项 | 路径 | 结果 | 证据 |
| --- | --- | --- | --- |
| 知识点标签 | `GET /courses/:id/practice/tags` | 通过 | 14 个标签 |
| 智能组卷 | `POST /courses/:id/practice/sessions` `mode=SMART` | 通过 | sessionId=`a13de4ad-815d-4c31-bc3c-9de4d403f7ca`，**10 题**（接口对 SMART 固定 10，请求里的 count=5 未生效，记入缺口） |
| 逐题作答 | `PATCH .../items/:itemId` | 通过 | 10 题均写入 |
| 提交批改 | `POST .../submit` | 通过，`GRADED` | **score=1.5/10**（选择题盲答第一选项） |
| 练习记录 | `GET .../practice/sessions/mine` | 通过 | count=2 |
| 错题本 | `GET /wrong-book/mine` | 通过 | 返回 `entries` |
| 辅导 | `POST .../items/:id/hint` | 通过，HTTP 200 | 未配大模型，返回本地模板「解题思路」 |
| 教师统计 | `GET .../practice/stats` | 通过 | `questionCount, feedbackByType, questions` |

未在本次展开：按标签组卷、错题组卷、自定义组卷、题目反馈处理闭环。列入测试缺口。

## UC08 实验讨论、提及、通知

| 检查项 | 路径 | 结果 | 证据 |
| --- | --- | --- | --- |
| 学生发帖并 `@` 教师 | `POST /labs/:labId/discussions` | 通过 | postId=`4b920e1e-522c-402f-b5f4-2c200c7de9de`，`mentionUserIds`=教师 id |
| 评论 | `POST .../comments` | 通过 | commentId=`65bc2ada-8877-481e-8452-51c60d15a214` |
| 教师通知 | `GET /notifications/unread-count`、`GET /notifications` | 通过 | unread **count=5**，存在 `type=DISCUSSION` |
| 讨论列表 | `GET /labs/:labId/discussions` | 通过 | posts=1，可见新帖 |

未在本次展开：匿名、置顶、已解决、附件、删除权限。列入测试缺口。

## 结论

UC06、UC07、UC08 的**主流程均可运行**。UC06 已证明 **Worker 消费评测任务**。三个用例均留下机器可读原始结果。
