# D1 现有测试脚本检查与缺口清单

- 日期：2026-08-25
- 负责人：吴本昭
- 对应任务：检查现有测试脚本，产出缺口清单（供 D3 补齐）

## 1. 资产盘点

| 路径 | 类型 | 与 UC06—UC08 关系 | 本次执行 |
| --- | --- | --- | --- |
| `backend/src/lib/lab-set-status.test.ts` | 单元测试（实验集时间窗） | UC06 窗口/补交规则 | **1/1 通过** |
| `tests/api/00-health.http` | REST Client，需人工 | 环境 | 未用 REST Client；等价接口已由脚本覆盖 |
| `tests/api/01-auth.http` | 同上 | 登录 | 脚本已登录 |
| `tests/api/03-homework-labs.http` | 同上 | UC06 提交/未开始/打回查询 | 逻辑已由 `d1-uc06-uc08-verify` 与 `retest-lab-submit` 覆盖 |
| `tests/api/04-practice-notify.http` | 同上 | UC07 组卷、UC08 通知列表 | 仅 GET/组卷，无答题提交、无讨论发帖 |
| `scripts/test-fixtures.mjs` | 固定 seed ID | 被多脚本引用 | 与 seed 输出一致 |
| `scripts/retest-lab-submit.mjs` | Node，评测联调 | UC06 | JS **ACCEPTED**；Python **ERROR** |
| `scripts/d1-uc06-uc08-verify.mjs` | 本次新增 D1 验证 | UC06—UC08 | **27/27 通过** |
| `scripts/step5-api-test.mjs` | 综合 API，会改选课期等 | 含实验/练习 | **未跑**（会 `PUT /enrollment/period` 关窗，干扰李璐曼 UC01） |
| `scripts/step6-system-test.mjs` | 系统测试 | 含 LAB/PRACTICE/NOTIFY | 已跑，exit 0；**TC-UX-001 失败**（前端 IPv4 不可达）；若干 LAB/PRACTICE 项标注「步骤5已验证」**并未真正再测评测** |
| `scripts/step8-perf-sec-test.mjs` | 性能/安全抽检 | 非 UC06—08 主路径 | 未作为本任务必跑 |
| `data/judge-demo/` | 手工自测包 | UC06 | 未走浏览器上传；API 已用等价 JS 代码 |
| `docs/范文歆/2026-8-25/04-测试报告.md` | 历史测试报告 | 过时（仍写 React 18 等） | 仅阅读 |

仓库内**没有** Playwright/Cypress 等浏览器 E2E（D3 胡思琪）。根目录**没有**统一 `npm test`。

## 2. 本次实际运行摘要

| 命令 | 结果 |
| --- | --- |
| `npm exec -- tsx --test src/lib/lab-set-status.test.ts`（backend） | pass 1 |
| `node scripts/d1-uc06-uc08-verify.mjs` | 27/27 |
| `node scripts/retest-lab-submit.mjs` | JS AC；Python ERROR |
| `node scripts/step6-system-test.mjs` | 进程成功；`webOk=false`，TC-UX-001 失败 |

`step6` 原始输出：`raw/step6-system-test.txt`（控制台重定向编码为乱码，JSON 字段名仍可读）。

## 3. 测试缺口（已并入 D3）

D1 当日盘点的缺口已不再在此更新。关闭项与剩余项见 [../2026-8-27/README.md](../2026-8-27/README.md)「缺口状态」。

D1 建议的四件事在 D3 的落实：`d1-uc06-uc08-verify.mjs` 已转发 `lab-regression.mjs`；回归不改选课期、不跑 step5/step6；Worker 用 JS 固定题 + `normalizeOutput` 单测。Python 回退仍未做。