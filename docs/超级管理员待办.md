# 超级管理员待办

> 本文档汇总平台「超级管理员」（代码中角色为 `ADMIN`，界面文案为「管理员」）相关改造任务。  
> 基于当前实现梳理：后端部分能力已具备，前端管理界面与权限边界仍不完整。

---

## 现状摘要

| 模块 | 后端 | 前端 | 备注 |
| --- | --- | --- | --- |
| 用户列表 | ✅ `GET /admin/users` | ⚠️ 只读列表 | 最多 500 条，无搜索分页 |
| 选课时段配置 | ✅ `GET/PUT /enrollment/period` | ✅ `/admin/enrollment` | 可配置阶段、开放时段、确认截止 |
| 手动加退课 | ✅ `POST /enrollment/admin/enroll`、`/drop` | ✅ `/admin/enrollment` | 日志含 `operatorId`，当前按 UUID 操作 |
| 课程容量调整 / 删除 | ✅ `PATCH/DELETE /enrollment/courses/:courseId` | ✅ `/admin/courses` | 支持容量、代码、学分、分类、发布状态与删除课程 |
| 全局课程/作业/实验视图 | ✅ 教师接口对 ADMIN 放行 | ✅ 教学台可见 | ADMIN 等同所有课授课教师 |
| 独立管理后台 | — | ❌ 无 | 导航混用学生端 + 教师端入口 |
| 角色变更 / 禁用用户 | ❌ 无 API | ❌ 无界面 | — |
| 操作审计日志 | ✅ `GET /admin/logs` + `AdminOperationLog` | ✅ `/admin/logs` | 记录删除用户、手动加退课、选课时段与课程字段调整 |

**演示账号**：`admin@demo.local`（见 `backend/prisma/seed.ts`）

**相关文件**

- 角色枚举：`backend/prisma/schema.prisma` → `enum Role { STUDENT TEACHER ADMIN }`
- 鉴权：`backend/src/lib/authGuard.ts`
- 课程权限：`backend/src/lib/courseAccess.ts`（`ADMIN` 视为所有课教师）
- 用户列表页：`frontend/src/pages/AdminUsers.tsx`
- 导航：`frontend/src/components/Shell.tsx`
- 选课占位：`frontend/src/pages/enrollment/EnrollmentPage.tsx`（admin Tab）

---

## P0 — 管理体验与核心缺口

### 1. 独立管理后台入口

- [x] 新增 `/admin` 控制台首页（概览：用户数、课程数、当前选课阶段等）
- [x] 管理后台子路由规划：
  - `/admin/users` — 用户管理（已有页面，需增强）
  - `/admin/enrollment` — 选课配置
  - `/admin/courses` — 课程运维
  - `/admin/logs` — 操作日志
- [x] 调整 `Shell.tsx`：`ADMIN` 默认只显示管理相关导航
- [x] 从管理员默认导航中移除「我的作业」「我的实验」等学生向入口

### 2. 选课管理前端（API 已就绪）

- [x] 选课时段配置表单：阶段（预选/正选/补退选/关闭）、开放时间、确认截止时间
  - 对接 `GET/PUT /enrollment/period`
- [x] 手动加课 / 退课：按用户 ID + 课程 ID
  - 对接 `POST /enrollment/admin/enroll`、`POST /enrollment/admin/drop`
  - 后续可增强为邮箱与课程搜索
- [x] 课程容量与选课字段编辑
  - 对接 `PATCH /enrollment/courses/:courseId`
- [x] 管理员删除课程
  - 对接 `DELETE /enrollment/courses/:courseId`
  - 删除前二次确认，并写入操作审计日志
- [ ] 替换 `EnrollmentPage.tsx` 中 admin Tab 占位文案为上述功能，或跳转至 `/admin/enrollment`

### 3. 角色与文案统一

- [ ] 产品确认：沿用 `ADMIN` 还是新增 `SUPER_ADMIN`
- [x] 统一界面文案（「管理员」→「超级管理员」，导航与控制台已更新）
- [ ] 文档与 seed 注释同步更新

---

## P1 — 用户管理与安全

### 4. 用户管理 CRUD

- [x] 后端：用户搜索 + 分页（替换 `take: 500` 硬限制）
  - `GET /admin/users?q=&role=&page=&pageSize=`
- [ ] 后端：修改用户角色 `PATCH /admin/users/:id/role`
  - 禁止修改自己的角色；禁止最后一个 ADMIN 被降级
- [ ] 后端：禁用 / 启用账号（需在 `User` 模型增加 `disabledAt` 或等价字段）
- [ ] 后端：管理员重置密码 `POST /admin/users/:id/reset-password`
- [ ] 前端：`AdminUsers.tsx` 增加搜索框、分页、角色下拉、操作按钮
- [ ] 前端：用户详情抽屉（邮箱验证状态、选课数、最近登录等，按需提供）

### 5. 操作审计

- [x] 设计 `AdminOperationLog` 表记录管理员操作
- [x] 记录：删除用户、删除课程、手动加退课、改容量/课程选课字段、阶段切换
- [x] 后端：`GET /admin/logs`（按操作人/目标、动作类型、目标类型筛选）
- [x] 前端：`/admin/logs` 列表页
- [ ] 后续随角色变更、重置密码、禁用账号功能补充对应日志

### 6. 生产安全

- [ ] 首次登录强制改密（或部署文档明确要求修改 seed 密码）
- [ ] 敏感操作二次确认（改角色、批量操作）
- [ ] 评估：管理接口是否需要 IP 白名单 / 更短 JWT 有效期（部署层配置）

---

## P2 — 权限细化与扩展

### 7. 业务边界澄清（需产品定稿）

- [ ] 明确超级管理员是否允许：批改作业、修改实验内容、代替教师发布资料
- [ ] 若不允许：后端将「平台运维」与「教学操作」拆分 middleware / 角色
- [ ] 若允许：UI 上区分「运维操作」与「以管理员身份介入教学」，避免误操作

### 8. 角色拆分（规模扩大后再做）

- [ ] 评估是否需要「院系管理员 / 课程管理员」等次级角色
- [ ] 若需要：扩展 `Role` 枚举 + 按模块授权（选课、用户、课程元数据、教学）

### 9. 批量与集成

- [ ] CSV 批量导入用户（学号、姓名、邮箱、角色、班级）
- [ ] 与教务系统对接的用户同步方案（文档级，实现另排期）

---

## 验收清单（联调时可勾选）

- [ ] 超级管理员登录后进入 `/admin`，不依赖 Postman 即可完成选课时段配置
- [ ] 可通过界面为指定学生手动加课 / 退课，且选课日志可查
- [ ] 用户列表支持搜索与分页，可修改非自身的用户角色
- [ ] 禁用账号后无法登录，已发 token 在合理时间内失效
- [x] 关键管理操作可在审计日志中追溯
- [ ] 导航结构清晰：管理职责与学生/教师日常使用不混淆

---

## 任务依赖关系（建议实施顺序）

```
角色/文案统一（P0-3）
    ↓
独立管理后台框架（P0-1）
    ↓
选课管理前端（P0-2）─────→ 操作审计（P1-5）
    ↓
用户管理 CRUD（P1-4）
    ↓
业务边界 + 角色拆分（P2，按产品节奏）
```

---

## 备注

- 选课相关需求亦见 `docs/选课系统.md` 管理员端描述。
- 若某任务已有部分实现，请在对应 `- [ ]` 改为 `- [x]` 并补充完成日期与 PR 链接。
