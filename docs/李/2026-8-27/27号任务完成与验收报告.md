# 2026-8-27 任务完成与验收报告

## 一、执行结论

27 号课程域自动化测试任务已经完成。UC01—UC04 共实现 16 个自动化检查项，覆盖四个用例的主流程、备选流程和异常流程；连续运行两次均为 16/16 通过，失败时脚本会返回非零退出码。

Kubernetes 清单已补齐 Ingress、Judge Worker 启动/存活探针和独立数据库迁移 Job，并通过本地静态渲染。由于本机当前没有可用的 Kubernetes 上下文，实际集群部署需要按《Kubernetes本地部署手工验收步骤.md》由操作者在 Docker Desktop 中手动启用 Kubernetes 后完成，本文不把未执行的集群验证标为通过。

补做及验收时间：2026-08-28 15:49—16:39（Asia/Shanghai）。

## 二、已完成内容

### 1. UC01—UC04 自动化测试

新增：

- `backend/src/tests/uc01-04.api.test.ts`
- 根命令 `npm run test:course`
- 后端命令 `npm run test:course --prefix backend`
- CI 步骤 `Run UC01-UC04 course-domain API tests`

测试使用固定演示账号：

- `teacher@demo.local`：课程、公告和资料的任课教师。
- `teacher2@demo.local`：其他教师，用于课程归属和越权检查。
- `student@demo.local`、`student02@demo.local`：选课、候补、公告及资料操作。
- `admin@demo.local`：读取、临时调整并恢复选课窗口。

每次运行创建标题带 `[UC27-AUTO]` 的唯一测试课程。脚本结束时删除测试课程、选课、候补、公告、已读、资料、收藏、日志及关联通知，删除上传的测试文件，并恢复运行前的选课窗口。

### 2. Kubernetes 清单

已完成：

- 新增 `k8s/monolith/ingress.yaml`，入口域名为 `teaching-platform.local`。
- Judge Worker 增加 `startupProbe` 和 `livenessProbe`。
- 新增 `k8s/monolith/migrate-job.yaml`，用于集群数据库执行 Prisma migration。
- `kustomization.yaml` 已纳入 Ingress。

数据库迁移 Job 没有直接放入 `kustomization.yaml`，目的是让操作者先等待 PostgreSQL Ready，再单独执行和查看迁移结果，最后部署完整应用，避免应用与迁移并发启动。

## 三、实际运行结果

| 验收项 | 命令/方式 | 结果 |
| --- | --- | --- |
| 数据库迁移 | `docker compose up -d db redis migrate` | PostgreSQL Healthy；migration Exit 0；25 个迁移，无待执行项 |
| API 就绪 | `GET http://127.0.0.1:3000/health/ready` | HTTP 200，PostgreSQL 与 Redis 均就绪 |
| UC01—UC04 第一次 | `npm run test:course` | 16/16 通过，cleanupError=null |
| UC01—UC04 第二次 | `npm run test:course` | 16/16 通过，cleanupError=null |
| 原有后端单元测试 | `npm run test:unit` | 通过，`lab-set-status.test.ts: ok` |
| 全项目生产构建 | `npm run build` | API、Web、Judge Worker 全部构建通过 |
| Kubernetes 静态渲染 | `kubectl kustomize k8s/monolith` | 通过，生成 Namespace、Service、PVC、Deployment、StatefulSet、Ingress |
| Kubernetes 实际部署 | Docker Desktop Kubernetes | 待操作者手动启用集群后验收 |

第二次自动化测试原始报告位于：

`test-results/uc01-04.json`

## 四、验收结论

| 要求 | 状态 | 说明 |
| --- | --- | --- |
| UC01 主/备选/异常流程有断言 | 通过 | 选课、退课同步、满员候补、自动递补、窗口关闭、重复操作及角色校验 |
| UC02 主/备选/异常流程有断言 | 通过 | 创建、配置、发布、重复代码、越权及非法参数 |
| UC03 主/备选/异常流程有断言 | 通过 | 发布、列表、阅读、唯一已读记录、切换未读及越权 |
| UC04 主/备选/异常流程有断言 | 通过 | 上传、浏览、预览、收藏、下载、筛选及越权/缺失文件 |
| 运行失败返回非零退出码 | 通过 | 任一检查失败、清理失败或覆盖不完整都会设置退出码 1 |
| 可重复运行 | 通过 | 连续两次均为 16/16，测试数据已清理，选课窗口已恢复 |
| 自动接入 CI | 通过 | API 启动后执行，JSON 报告进入 `test-results` Artifact |
| Kubernetes 清单静态有效 | 通过 | Kustomize 渲染成功 |
| Kubernetes Pod/Service 实机验证 | 待手工验收 | 当前无 Kubernetes context，不能声明实际部署通过 |

## 五、环境说明

启动 Compose Redis 时出现宿主机 6379 端口已占用。检查显示该端口由 Docker Desktop 后端代理监听，实际 API `/health/ready` 返回 200，说明 Redis 可用。本次没有停止、删除或覆盖该现有服务。

测试结束后，本次启动的本地 API 进程已停止；Compose PostgreSQL 保持 Healthy，migration 容器保持 Exit 0，未删除任何现有数据卷。

前端构建存在单个 JavaScript 包超过 500 kB 的 Vite 警告，但构建成功。这是性能优化提示，不影响本次 UC01—UC04 和 Kubernetes 清单验收。
