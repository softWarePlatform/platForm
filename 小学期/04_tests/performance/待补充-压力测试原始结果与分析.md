# 压力测试材料已补充

完整材料见 [`压力测试结果与分析.md`](压力测试结果与分析.md)，包括：

- 可复跑脚本 `run-performance.ps1` 和 `performance-benchmark.mjs`；
- 不含真实 Token 的 `benchmark-config.example.json`；
- `raw-results/` 下单体与微服务各 3 轮逐请求原始 JSON；
- 测试前后 Kubernetes Pod CPU/内存快照；
- 三轮数据表、中位数对比、波动及限流说明。
