# C 站会：2026-09-02（D8）

## 今日工作内容（简）

D8 主题：把权限切到 A 已冻结的内部接口；降级与网关 502 再验一次；错题仍等 B。

## 今日计划

1. 权限改走 A 的 `GET /internal/courses/:id/access/:userId`（写操作 fail-closed），名单已兼容 `items`。
2. 通知确认只认 `Idempotency-Key` 头（A 缺头会 400）；与 A 对齐 token。
3. 经网关再跑作业冒烟；修 `final-gradebook` 偶发卡住。
4. B 若给出 `lab-gradebook` / 错题 API：切真调用，去掉 501。
5. 单测/API 测试补洞（能做的先做，不等 Lab 进程）。

## 当前阻塞

| 阻塞 | 找谁 |
| --- | --- |
| Lab 独立进程 `:3003` 未起，综合成绩仍是降级（`labStatus=UNAVAILABLE`），错题 PUT 写得进客户端但联调不到真服务 | B 起 Lab |
| 三服务经 Gateway 的实验路径 200（现在只能测 502） | B 起 `:3003` |
| `INTERNAL_SERVICE_TOKEN` | 已与 A 对齐为 `replace-with-service-token`（两边 `.env` 同一串） |

A 内部接口 **今日已冻结**（名单 `items`、access、通知），不再阻塞权限切换。
